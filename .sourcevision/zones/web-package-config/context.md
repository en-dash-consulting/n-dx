# Web Package Config — Zone Context

<!-- Per-zone context generated by Sourcevision. -->

<zone>

Zone: Web Package Config (`web-package-config`)
Files: 2, Cohesion: 0.33, Coupling: 0.67
Risk: critical (score: 0.67)
Description: Build infrastructure for the web package: esbuild runner scripts, TypeScript configuration, Vitest config, and package manifest — the configuration layer that orchestrates viewer compilation and test execution.
Entry points: packages/web/src/viewer/components/elapsed-time.ts, packages/web/src/viewer/polling/tick-timer.ts, packages/web/src/viewer/route-state.ts, packages/web/src/viewer/usage/constants.ts, packages/web/src/viewer/views/task-audit.ts, packages/web/src/viewer/views/token-usage.ts
Lines: 352

</zone>

<files>

packages/web/build.js (JavaScript, 238 lines, source)
packages/web/dev.js (JavaScript, 114 lines, source)

</files>

<findings>

[observation] [info] Once viewer files are correctly pinned, this zone becomes a pure 4-file config island (build.js, dev.js, package.json, tsconfig.json, vitest.config.ts) with cohesion near 0 by design — no further remediation is needed.
[observation] [warning] The viewer entry points (elapsed-time.ts, route-state.ts, tick-timer.ts, task-audit.ts, token-usage.ts, usage/constants.ts) should be pinned to web-viewer; their presence here inflates coupling to 0.67 and creates a false cross-zone dependency signal.
[observation] [info] build.js and dev.js are executable runner scripts, not static config — classifying them as [config] archetype understates their role; marking them as [entrypoint] would align with their actual function as the build pipeline entry.
[suggestion] [info] Zone "Web Package Config" (web-package-config) has critical risk (score: 0.67, cohesion: 0.33, coupling: 0.67) — unreliable: zone has only 2 files (minimum 5 for reliable metrics)

</findings>

<insights>

- The six viewer files listed as entry points (elapsed-time.ts, route-state.ts, tick-timer.ts, task-audit.ts, token-usage.ts, usage/constants.ts) are misclassified here — per developer hints they belong in the web-viewer zone alongside the rest of the Preact UI code.
- Low cohesion (0.33) is inherent and expected: build/config files do not import each other, so import-graph community detection has no signal to raise the score — this is documented as acceptable in the zone hints.
- High coupling (0.67) is a warning-level artifact of the misclassified viewer entry points pulling in web-viewer import edges; resolving the classification should bring coupling down to near zero for a pure config zone.
- The viewer entry points (elapsed-time.ts, route-state.ts, tick-timer.ts, task-audit.ts, token-usage.ts, usage/constants.ts) should be pinned to web-viewer; their presence here inflates coupling to 0.67 and creates a false cross-zone dependency signal.
- build.js and dev.js are executable runner scripts, not static config — classifying them as [config] archetype understates their role; marking them as [entrypoint] would align with their actual function as the build pipeline entry.
- Once viewer files are correctly pinned, this zone becomes a pure 4-file config island (build.js, dev.js, package.json, tsconfig.json, vitest.config.ts) with cohesion near 0 by design — no further remediation is needed.
- [call graph] 25 internal calls, 0 outgoing, 0 incoming (cohesion: 1, coupling: 0)

</insights>
