# Landing Page — Zone Context

<!-- Per-zone context generated by Sourcevision. -->

<zone>

Zone: Landing Page (`landing-page`)
Files: 3, Cohesion: 0.00, Coupling: 0.00
Risk: at-risk (score: 0.50)
Description: Self-contained static landing page with its own HTML, CSS, and TypeScript entry point, bundled independently from the main dashboard.
Lines: 1977

</zone>

<files>

packages/web/src/landing/index.html (HTML, 309 lines, other)
packages/web/src/landing/landing.css (CSS, 1317 lines, other)
packages/web/src/landing/landing.ts (TypeScript, 351 lines, source)

</files>

<findings>

[observation] [info] Isolated files — no import edges between 3 files, cohesion is unmeasurable (reported as 0)
[observation] [info] No cross-zone imports into or out of this zone — if landing.ts needs shared utilities in the future, ensure they are explicitly imported rather than relying on globals.
[observation] [info] Perfect isolation (cohesion: 0, coupling: 0) confirms this landing page is cleanly separated from application logic — appropriate for a marketing or splash page.
[observation] [info] Zero cohesion is expected here since HTML and CSS files have no import relationships; the metric is not meaningful for static asset zones.
[pattern] [info] landing-page and viewer-static-assets form a matched pair of terminal zones (zero coupling, zero cohesion) — they can be versioned, replaced, or deleted independently without impacting any other zone.
[suggestion] [info] Zone "Landing Page" (landing-page) has at-risk risk (score: 0.50, cohesion: 0.00, coupling: 0.00) — approaching architectural risk thresholds

</findings>

<insights>

- Isolated files — no import edges between 3 files, cohesion is unmeasurable (reported as 0)
- Zero cohesion and zero coupling indicate this zone is entirely isolated — it neither imports from nor is imported by any other zone.
- With only 3 files (HTML, CSS, TS), this is a minimal but well-separated concern.
- No entry points are detected, suggesting the bundler resolves this via direct HTML reference rather than JS module graph traversal.
- Perfect isolation (cohesion: 0, coupling: 0) confirms this landing page is cleanly separated from application logic — appropriate for a marketing or splash page.
- Zero cohesion is expected here since HTML and CSS files have no import relationships; the metric is not meaningful for static asset zones.
- No cross-zone imports into or out of this zone — if landing.ts needs shared utilities in the future, ensure they are explicitly imported rather than relying on globals.
- landing-page and viewer-static-assets together form a 'terminal zone' pattern — both are pure leaf nodes with zero coupling in either direction, confirming they are correctly isolated from application logic and will never introduce transitive dependencies.
- landing-page and viewer-static-assets form a matched pair of terminal zones (zero coupling, zero cohesion) — they can be versioned, replaced, or deleted independently without impacting any other zone.
- landing-page is the only zone with no path to the composition root (start.ts) through any import chain — if it is served by the same web server, that wiring is done entirely outside the JS module graph (e.g. via static file middleware), making it invisible to any import-based architectural analysis or enforcement tool.
- [call graph] 17 internal calls, 0 outgoing, 0 incoming (cohesion: 1, coupling: 0)

</insights>
