# Web Landing — Zone Context

<!-- Per-zone context generated by Sourcevision. -->

<zone>

Zone: Web Landing (`web-landing`)
Files: 3, Cohesion: 1.00, Coupling: 0.00
Risk: healthy (score: 0.00)
Description: 5 files, primarily TypeScript, HTML, CSS
Lines: 1977

</zone>

<files>

packages/web/src/landing/index.html (HTML, 309 lines, other)
packages/web/src/landing/landing.css (CSS, 1317 lines, other)
packages/web/src/landing/landing.ts (TypeScript, 351 lines, source)

</files>

<findings>

[observation] [info] High cohesion (1) — files are tightly interconnected

</findings>

<insights>

- High cohesion (1) — files are tightly interconnected
- Perfect cohesion (1.0) and zero coupling confirms this is correctly isolated — changes to landing page assets cannot ripple into the viewer or server layers.
- The zone contains exactly three files (HTML, CSS, TS entrypoint), which is the minimal viable structure for a standalone page; no bloat risk here.
- Because this zone has no entrypoints exported (none listed), it is consumed only via the build pipeline — verify that the build config correctly bundles landing.ts as a separate chunk from the viewer bundle.
- Zero coupling to the rest of the web package is ideal — the landing page can be independently deployed or replaced without touching dashboard or server code.
- landing.ts is classified as an entrypoint in the file list but no entryPoints are detected algorithmically — confirm the build pipeline explicitly targets this file to avoid it being tree-shaken away.
- At 3 files this zone will remain stable; no architectural action needed beyond ensuring the landing page build output path is distinct from the viewer bundle.
- Has zero coupling to viewer-html-shell despite both being static-asset-heavy zones in the same package — they are correctly separated by concern (marketing page vs. app shell) with no shared assets or scripts.
- landing-page and viewer-html-shell share no imports despite both being HTML/asset zones; the boundary between marketing surface and app shell is clean and should be preserved if either zone grows.
- [call graph] 17 internal calls, 0 outgoing, 0 incoming (cohesion: 1, coupling: 0)

</insights>
