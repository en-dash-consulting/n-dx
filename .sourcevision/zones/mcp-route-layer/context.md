# MCP Route Layer — Zone Context

<!-- Per-zone context generated by Sourcevision. -->

<zone>

Zone: MCP Route Layer (`mcp-route-layer`)
Files: 4, Cohesion: 0.40, Coupling: 0.60
Risk: healthy (score: 0.60)
Description: The MCP HTTP route handler and its domain gateway, forming the thin composition layer that wires sourcevision MCP tool calls into the unified web server.
Entry points: packages/web/src/server/routes-mcp.ts
Lines: 598

</zone>

<files>

packages/web/src/server/domain-gateway.ts (TypeScript, 19 lines, source)
packages/web/src/server/routes-mcp.ts (TypeScript, 226 lines, source)
packages/web/tests/unit/server/domain-gateway.test.ts (TypeScript, 28 lines, test)
packages/web/tests/unit/server/routes-mcp.test.ts (TypeScript, 325 lines, test)

</files>

<imports>

Internal:
  packages/web/src/server/routes-mcp.ts → packages/web/src/server/domain-gateway.ts {createSourcevisionMcpServer}
  packages/web/tests/unit/server/domain-gateway.test.ts → packages/web/src/server/domain-gateway.ts {createSourcevisionMcpServer}
  packages/web/tests/unit/server/routes-mcp.test.ts → packages/web/src/server/routes-mcp.ts {handleMcpRoute, closeAllMcpSessions}

Outgoing (this zone → other zones):
  → packages-sourcevision:unit: packages/web/tests/unit/server/domain-gateway.test.ts → packages/sourcevision/src/public.ts
  → web-viewer-application: packages/web/src/server/routes-mcp.ts → packages/web/src/server/rex-gateway.ts; packages/web/src/server/routes-mcp.ts → packages/web/src/server/types.ts; packages/web/tests/unit/server/routes-mcp.test.ts → packages/web/src/server/types.ts

Incoming (other zones → this zone):
  ← web-viewer-application: packages/web/src/server/start.ts → packages/web/src/server/routes-mcp.ts; packages/web/tests/unit/server/memory-leak-fixes.test.ts → packages/web/src/server/routes-mcp.ts

</imports>

<findings>

[observation] [warning] High coupling (0.6) — 3 imports target "web-viewer-application"
[observation] [info] Coupling of 0.6 reaches the warning threshold but is explicitly justified by the composition-root wiring pattern documented in the project context hints; no structural change is needed.
[observation] [info] The bidirectional cross-zone imports with web-viewer-application (3 outbound, 2 inbound) should be kept stable; any new import paths added here must flow through the existing gateway rather than bypassing it.
[observation] [info] The zone's small size (4 files) and focused responsibility (MCP route handling + domain gateway) are signs of good single-responsibility design.
[pattern] [info] mcp-route-layer cohesion (0.4) is lower than both tiers it connects (web-viewer-application: 0.98, web-shared-foundation: 0.44) — this is the expected signature of a composition-root zone whose purpose is wiring, not encapsulation.
[relationship] [info] mcp-route-layer has no direct imports from web-shared-foundation, confirming it respects the layer-skip prohibition; all foundation access flows through web-viewer-application as the intermediate tier.
[anti-pattern] [info] The composition-root zone (mcp-route-layer) is incomplete: it contains the MCP route handler (routes-mcp.ts) and the sourcevision gateway (domain-gateway.ts) but not the server entry point (start.ts) that wires the handler into Express. As a result, the zone records an inbound import from web-viewer-application (start.ts → routes-mcp.ts), which is reported as an upward crossing and misrepresents the actual dependency direction. start.ts should be pinned to mcp-route-layer to make the zone a correct, complete composition root.
[suggestion] [info] Rename the zone to 'web-composition-root' or 'web-server-wiring' to accurately reflect its architectural role (Express route registration and cross-package gateway wiring) rather than a single transport protocol. 'mcp-route-layer' overpromises MCP ownership that the zone does not fully deliver — rex-gateway.ts (the Rex MCP factory) is in web-viewer-application.

</findings>

<insights>

- High coupling (0.6) — 3 imports target "web-viewer-application"
- Coupling of 0.6 at the zone boundary is architectural rather than incidental — routes-mcp.ts must reach rex-gateway and types.ts, and start.ts in web-dashboard must import the route handler; this bidirectional coupling is documented and intentional.
- At only 4 files (2 production, 2 tests) the zone is correctly minimal — it owns exactly one concern: translating HTTP MCP requests into domain gateway calls.
- The 3 imports from web-server into web-viewer and 2 in the reverse direction represent the composition-root pattern; these should be reviewed during any refactor to ensure they remain type-only or gateway-mediated.
- Coupling of 0.6 reaches the warning threshold but is explicitly justified by the composition-root wiring pattern documented in the project context hints; no structural change is needed.
- The zone's small size (4 files) and focused responsibility (MCP route handling + domain gateway) are signs of good single-responsibility design.
- The bidirectional cross-zone imports with web-viewer-application (3 outbound, 2 inbound) should be kept stable; any new import paths added here must flow through the existing gateway rather than bypassing it.
- The mcp-route-layer sits exclusively at the top of the web package's three-tier stack — it imports from web-viewer-application (below) but has no imports from web-shared-foundation (two tiers below), confirming it delegates foundation concerns to the application layer rather than reaching past it.
- The zone's 0.4 cohesion (versus 0.98 for web-viewer-application and 0.44 for web-shared-foundation) reflects that it is a wiring zone rather than a feature zone — low cohesion is structurally correct for composition roots.
- mcp-route-layer has no direct imports from web-shared-foundation, confirming it respects the layer-skip prohibition; all foundation access flows through web-viewer-application as the intermediate tier.
- mcp-route-layer cohesion (0.4) is lower than both tiers it connects (web-viewer-application: 0.98, web-shared-foundation: 0.44) — this is the expected signature of a composition-root zone whose purpose is wiring, not encapsulation.
- mcp-route-layer currently owns routes-mcp.ts and domain-gateway.ts but not start.ts, the file that actually wires routes-mcp.ts into the Express server. This means the composition-root zone is missing its true entry point, and the wiring file (start.ts) is stranded in the application layer below. The zone would be architecturally complete and correctly named if start.ts were pinned here alongside the handlers it wires.
- routes-mcp.ts imports createRexMcpServer from rex-gateway.ts (which is in web-viewer-application). This is a correct downward import — the handler pulls its MCP server factory from the gateway in the application layer. The 3 mcp-route-layer → web-viewer-application crossings documented in zone metrics include this edge; it is not a violation but should be verified to flow exclusively through rex-gateway.ts and domain-gateway.ts rather than any leaf server file.
- The composition-root zone (mcp-route-layer) is incomplete: it contains the MCP route handler (routes-mcp.ts) and the sourcevision gateway (domain-gateway.ts) but not the server entry point (start.ts) that wires the handler into Express. As a result, the zone records an inbound import from web-viewer-application (start.ts → routes-mcp.ts), which is reported as an upward crossing and misrepresents the actual dependency direction. start.ts should be pinned to mcp-route-layer to make the zone a correct, complete composition root.
- The zone name 'mcp-route-layer' implies full ownership of the MCP layer, but the Rex MCP server factory (rex-gateway.ts) lives in web-viewer-application. The zone owns the HTTP handler and the sourcevision gateway but not the Rex factory the handler depends on — a naming scope mismatch that makes it harder for contributors to locate all MCP-related files.
- The two production files in this zone (routes-mcp.ts and domain-gateway.ts) likely have no direct import edge between each other — domain-gateway.ts serves sourcevision while routes-mcp.ts depends on rex-gateway.ts (in web-viewer-application). The zone's cohesion of 0.4 derives from shared test proximity, not a production import relationship. The zone is cohesive by test co-location, not by domain import graph.
- mcp-route-layer has no direct imports from web-shared-foundation, meaning the composition root cannot access shared constants without traversing the 387-file application layer. The indirect access chain (mcp-route-layer → web-viewer-application → web-shared-foundation) means the composition root cannot be extracted or independently tested without also migrating foundation access.
- Rename the zone to 'web-composition-root' or 'web-server-wiring' to accurately reflect its architectural role (Express route registration and cross-package gateway wiring) rather than a single transport protocol. 'mcp-route-layer' overpromises MCP ownership that the zone does not fully deliver — rex-gateway.ts (the Rex MCP factory) is in web-viewer-application.
- [call graph] 14 internal calls, 1 outgoing, 3 incoming (cohesion: 0.93, coupling: 0.07)

</insights>
