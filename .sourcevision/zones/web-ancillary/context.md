# Web Ancillary — Zone Context

<!-- Per-zone context generated by Sourcevision. -->

<zone>

Zone: Web Ancillary (`web-ancillary`)
Files: 2, Cohesion: 0.00, Coupling: 0.00
Risk: at-risk (score: 0.50)
Description: A residual grouping of loosely related web-package support files: the landing page service, integration boundary tests, and ad-hoc analysis scripts that fall outside the main web-dashboard community.
Lines: 207

</zone>

<files>

scripts/hench-callgraph-analysis.mjs (JavaScript, 206 lines, build)
scripts/parse-gateway-exports.mjs (JavaScript, 1 lines, build)

</files>

<findings>

[observation] [info] Isolated files — no import edges between 2 files, cohesion is unmeasurable (reported as 0)
[observation] [warning] Zero cohesion is expected for a residual zone, but mixing production service code (landing.ts), integration tests, and developer scripts in one zone makes health metrics unreliable for all three file categories.
[observation] [info] scripts/hench-callgraph-analysis.mjs and scripts/parse-gateway-exports.mjs belong to project-root tooling, not the web package — pinning them to the project-root-config zone would dissolve this residual zone's cross-concern contamination.
[suggestion] [info] Zone "Web Ancillary" (web-ancillary) has at-risk risk (score: 0.50, cohesion: 0.00, coupling: 0.00) — unreliable: zone has only 2 files (minimum 5 for reliable metrics)

</findings>

<insights>

- Isolated files — no import edges between 2 files, cohesion is unmeasurable (reported as 0)
- Zero cohesion (0.0) and zero coupling confirm this is a Louvain residual zone — the files do not import each other and are only weakly connected to the rest of the graph.
- The two scripts (hench-callgraph-analysis.mjs, parse-gateway-exports.mjs) are developer tooling, not runtime web code; they would be better grouped with project-root-config or docs zones.
- landing.ts and its test are a small self-contained module; if the landing page grows, it warrants its own zone. Currently it is too small to split meaningfully.
- Zero cohesion is expected for a residual zone, but mixing production service code (landing.ts), integration tests, and developer scripts in one zone makes health metrics unreliable for all three file categories.
- scripts/hench-callgraph-analysis.mjs and scripts/parse-gateway-exports.mjs belong to project-root tooling, not the web package — pinning them to the project-root-config zone would dissolve this residual zone's cross-concern contamination.
- [call graph] 13 internal calls, 0 outgoing, 0 incoming (cohesion: 1, coupling: 0)

</insights>
