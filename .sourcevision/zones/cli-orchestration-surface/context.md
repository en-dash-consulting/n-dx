# CLI Orchestration Surface — Zone Context

<!-- Per-zone context generated by Sourcevision. -->

<zone>

Zone: CLI Orchestration Surface (`cli-orchestration-surface`)
Files: 6, Cohesion: 1.00, Coupling: 0.00
Description: Top-level scripts that broker every n-dx workflow by spawning package CLIs and presenting a cohesive developer UX.
Lines: 3103

</zone>

<files>

ci.js (JavaScript, 269 lines, source)
cli.js (JavaScript, 393 lines, source)
config.js (JavaScript, 910 lines, source)
help.js (JavaScript, 870 lines, source)
tests/unit/help.test.js (JavaScript, 252 lines, test)
web.js (JavaScript, 409 lines, source)

</files>

<imports>

Internal:
  cli.js → ci.js {runCI}
  cli.js → config.js {runConfig}
  cli.js → help.js {formatTypoSuggestion, getOrchestratorCommands, searchHelp, formatSearchResults, formatToolHelp, formatMainHelp, formatOrchestratorCommandHelp}
  cli.js → web.js {runWeb}
  tests/unit/help.test.js → help.js {editDistance, suggestCommands, formatTypoSuggestion, searchHelp, formatSearchResults, getOrchestratorCommands, getToolSubcommands, formatToolHelp, getRelatedCommands, formatRelatedCommands}

</imports>

<findings>

[observation] [info] High cohesion (1) — files are tightly interconnected
[observation] [info] Documented four-tier dependency rules in `cli.js:3` keep orchestration decoupled from execution; add an automated import-lint or test so future entrypoints cannot bypass the spawn-only contract.
[observation] [info] Secret-aware config writing in `config.js:49` already chmods `.n-dx.json` when API keys exist, so route any new sensitive settings through this helper instead of ad-hoc package config writers to keep credentials locked down.
[observation] [info] The command registry/search UX in `help.js:83` and its regression coverage in `tests/unit/help.test.js:4` should be treated as required touchpoints whenever commands are added or renamed to keep typo suggestions and docs accurate.

</findings>

<insights>

- High cohesion (1) — files are tightly interconnected
- Holds the entrypoints (CLI, CI, web) that enforce the repo's four-tier dependency layering.
- Shares config/help/test helpers so onboarding and diagnostics feel the same across commands.
- Documented four-tier dependency rules in `cli.js:3` keep orchestration decoupled from execution; add an automated import-lint or test so future entrypoints cannot bypass the spawn-only contract.
- Secret-aware config writing in `config.js:49` already chmods `.n-dx.json` when API keys exist, so route any new sensitive settings through this helper instead of ad-hoc package config writers to keep credentials locked down.
- The command registry/search UX in `help.js:83` and its regression coverage in `tests/unit/help.test.js:4` should be treated as required touchpoints whenever commands are added or renamed to keep typo suggestions and docs accurate.
- [call graph] 284 internal calls, 0 outgoing, 0 incoming (cohesion: 1, coupling: 0)

</insights>
