# Root Orchestration — Zone Context

<!-- Per-zone context generated by Sourcevision. -->

<zone>

Zone: Root Orchestration (`root-orchestration`)
Files: 3, Cohesion: 1.00, Coupling: 0.00
Description: Top-level CLI entry points and configuration for the n-dx monorepo, delegating to individual packages.
Lines: 896

</zone>

<files>

ci.js (JavaScript, 241 lines, source)
cli.js (JavaScript, 186 lines, source)
config.js (JavaScript, 469 lines, source)

</files>

<imports>

Internal:
  cli.js → ci.js {runCI}
  cli.js → config.js {runConfig}

</imports>

<findings>

[observation] [info] High cohesion (1) — files are tightly interconnected
[observation] [info] Only 3 files suggests this zone is appropriately minimal for its coordination role
[observation] [info] Perfect cohesion (1.0) with zero coupling indicates well-isolated orchestration layer
[pattern] [info] Orchestration layer delegates entirely through CLI spawning rather than direct imports, creating a process boundary that prevents accidental tight coupling

</findings>

<insights>

- High cohesion (1) — files are tightly interconnected
- Serves as the unified interface layer that coordinates sourcevision, rex, and hench packages
- Configuration is centralized here for cross-package settings management
- Zero inbound coupling from package zones — clean facade pattern isolating consumers from implementation details
- Entry points delegate immediately to package CLIs — minimal business logic reduces maintenance burden
- All three files follow consistent naming: lowercase single-word filenames with .js extension — no .ts or hyphenated names at orchestration layer

</insights>
