# CLI End-to-End Tests — Zone Context

<!-- Per-zone context generated by Sourcevision. -->

<zone>

Zone: CLI End-to-End Tests (`cli-e2e-tests`)
Files: 22, Cohesion: 1.00, Coupling: 0.00
Risk: healthy (score: 0.00)
Description: End-to-end test suite that exercises the n-dx orchestration CLI commands, delegation contracts, architecture policies, and error paths against real spawned processes.
Lines: 6292

</zone>

<files>

tests/e2e/architecture-policy.test.js (JavaScript, 307 lines, test)
tests/e2e/cli-arg-contracts.test.js (JavaScript, 320 lines, test)
tests/e2e/cli-ci.test.js (JavaScript, 447 lines, test)
tests/e2e/cli-config.test.js (JavaScript, 1044 lines, test)
tests/e2e/cli-contract.test.js (JavaScript, 99 lines, test)
tests/e2e/cli-delegation.test.js (JavaScript, 65 lines, test)
tests/e2e/cli-dev.test.js (JavaScript, 30 lines, test)
tests/e2e/cli-errors.test.js (JavaScript, 126 lines, test)
tests/e2e/cli-init.test.js (JavaScript, 264 lines, test)
tests/e2e/cli-orchestration.test.js (JavaScript, 401 lines, test)
tests/e2e/cli-output-contracts.test.js (JavaScript, 85 lines, test)
tests/e2e/cli-pr-check.test.js (JavaScript, 325 lines, test)
tests/e2e/cli-refresh.test.js (JavaScript, 307 lines, test)
tests/e2e/cli-spawn-boundary.test.js (JavaScript, 169 lines, test)
tests/e2e/cli-start.test.js (JavaScript, 312 lines, test)
tests/e2e/cli-tool-resolution.test.js (JavaScript, 55 lines, test)
tests/e2e/cli-web.test.js (JavaScript, 321 lines, test)
tests/e2e/domain-isolation.test.js (JavaScript, 939 lines, test)
tests/e2e/e2e-helpers.js (JavaScript, 283 lines, test)
tests/e2e/integration-coverage-policy.test.js (JavaScript, 119 lines, test)
tests/e2e/mcp-transport.test.js (JavaScript, 228 lines, test)
tests/e2e/verify-build.js (JavaScript, 46 lines, test)

</files>

<imports>

Internal:
  tests/e2e/cli-arg-contracts.test.js → tests/e2e/e2e-helpers.js {run}
  tests/e2e/cli-ci.test.js → tests/e2e/e2e-helpers.js {runBase, runResultBase, createTmpDir, removeTmpDir, setupRexDir, setupSourcevisionDir}
  tests/e2e/cli-delegation.test.js → tests/e2e/e2e-helpers.js {run, runFail}
  tests/e2e/cli-dev.test.js → tests/e2e/e2e-helpers.js {run, runResult, createTmpDir, removeTmpDir}
  tests/e2e/cli-errors.test.js → tests/e2e/e2e-helpers.js {run, runFail}
  tests/e2e/cli-output-contracts.test.js → tests/e2e/e2e-helpers.js {createTmpDir, removeTmpDir, setupRexDir, setupSourcevisionDir, CLI_PATH}
  tests/e2e/mcp-transport.test.js → tests/e2e/e2e-helpers.js {createTmpDir, removeTmpDir, setupRexDir, setupSourcevisionDir}

</imports>

<findings>

[observation] [info] High cohesion (1) — files are tightly interconnected
[observation] [info] Perfect cohesion (1.0) and zero coupling confirms the e2e suite is fully isolated from production code modules, relying only on CLI spawn behavior — this is the correct pattern for orchestration-tier tests.
[observation] [info] architecture-policy.test.js enforcing gateway and tier-boundary rules at the e2e layer is a strong architectural safeguard; it should be run in CI before any merge to main to catch boundary violations early.
[observation] [info] cli-delegation.test.js validates that orchestration scripts spawn sub-CLIs rather than importing them directly — this is the automated guard for the spawn-vs-gateway decision rule and must remain in the suite as packages evolve.
[pattern] [warning] The MCP HTTP transport path (ndx start → /mcp/rex, /mcp/sourcevision endpoints) has no e2e test coverage; the stdio transport path is validated indirectly through delegation tests, creating an asymmetric risk as HTTP becomes the recommended transport.
[relationship] [warning] architecture-policy.test.js and the cross-package-integration-tests zone together form complementary enforcement layers (static import rules + runtime wiring), but the gap between them — runtime gateway shape correctness — is uncovered by either zone.
[anti-pattern] [warning] The architecture-policy test enforces gateway and tier-boundary rules via static source analysis but has no runtime assertion that gateway files contain only re-exports and no logic; a gateway that wraps a function with added behavior (e.g. logging, error transformation) passes all static checks while silently violating the re-export-only contract.
[suggestion] [warning] No e2e test asserts that package-tier modules (rex, sourcevision, hench) never shell-exec or spawn orchestration-tier scripts (cli.js, web.js, ci.js). While import-graph enforcement catches module-import inversions, process-spawn inversions from packages back into orchestration scripts are undetectable by static analysis. Add a test in cli-e2e-tests that scans package src/ directories for shell-exec or spawn calls targeting the monorepo root scripts.
[suggestion] [warning] config.js is the only orchestration-tier script with a documented exception to the spawn-only rule (it reads package config files directly). This exception has no automated test — a future refactor converting config.js to spawn-based delegation would silently break cross-package atomic config reads without failing any test. Add a test case to cli-e2e-tests that exercises a cross-package config read and asserts the result is atomically consistent (e.g. both rex and hench config values appear in a single invocation output).

</findings>

<insights>

- High cohesion (1) — files are tightly interconnected
- 22 files covering CLI contracts, delegation, dev mode, config, and architecture policy gives strong regression coverage for the orchestration tier where integration is otherwise hard to unit-test.
- architecture-policy.test.js is strategically important — it machine-enforces the four-tier import hierarchy and gateway rules, effectively acting as a living architecture fitness function.
- cli-arg-contracts.test.js and cli-contract.test.js may overlap in scope; periodically audit for duplicate coverage and consolidate to reduce maintenance burden.
- Perfect cohesion (1.0) and zero coupling confirms the e2e suite is fully isolated from production code modules, relying only on CLI spawn behavior — this is the correct pattern for orchestration-tier tests.
- architecture-policy.test.js enforcing gateway and tier-boundary rules at the e2e layer is a strong architectural safeguard; it should be run in CI before any merge to main to catch boundary violations early.
- cli-delegation.test.js validates that orchestration scripts spawn sub-CLIs rather than importing them directly — this is the automated guard for the spawn-vs-gateway decision rule and must remain in the suite as packages evolve.
- cli-e2e-tests validates spawn-based delegation at the process boundary but provides no coverage of the HTTP MCP transport layer — session lifecycle, Streamable HTTP compliance, and concurrent MCP request handling are exercised only through manual testing or not at all.
- The architecture-policy test enforces import hierarchy statically via source analysis but cannot detect runtime violations such as a gateway that wraps rather than purely re-exports a function, silently changing call semantics without violating any import rule.
- The MCP HTTP transport path (ndx start → /mcp/rex, /mcp/sourcevision endpoints) has no e2e test coverage; the stdio transport path is validated indirectly through delegation tests, creating an asymmetric risk as HTTP becomes the recommended transport.
- architecture-policy.test.js and the cross-package-integration-tests zone together form complementary enforcement layers (static import rules + runtime wiring), but the gap between them — runtime gateway shape correctness — is uncovered by either zone.
- 447 internal calls across 22 files (~20 calls/file) indicates significant shared utility usage within the e2e suite; if shared helpers encapsulate CLI spawn logic, test isolation is preserved, but if they share mutable process-level state (e.g. environment variable patches), a single flaky test could corrupt the environment for all subsequent tests in the same run.
- The architecture-policy test enforces gateway and tier-boundary rules via static source analysis but has no runtime assertion that gateway files contain only re-exports and no logic; a gateway that wraps a function with added behavior (e.g. logging, error transformation) passes all static checks while silently violating the re-export-only contract.
- cli-e2e-tests validates that orchestration scripts spawn sub-CLIs rather than importing them directly, but there is no test verifying the reverse — that no package-tier module ever spawns or shell-execs an orchestration-tier script (cli.js, web.js, ci.js). Package code calling back into orchestration scripts would invert the four-tier hierarchy at the process level, invisible to import-graph analysis.
- The spawn-exempt config.js exception — the only orchestration-tier script permitted to read package files directly rather than spawning — has no dedicated test case in cli-e2e-tests that validates it uses direct file I/O rather than spawning. The exception is prose-only in CLAUDE.md, meaning a refactor that accidentally converts config.js to spawn-based delegation would pass all tests while silently breaking cross-package atomic config reads.
- No e2e test asserts that package-tier modules (rex, sourcevision, hench) never shell-exec or spawn orchestration-tier scripts (cli.js, web.js, ci.js). While import-graph enforcement catches module-import inversions, process-spawn inversions from packages back into orchestration scripts are undetectable by static analysis. Add a test in cli-e2e-tests that scans package src/ directories for shell-exec or spawn calls targeting the monorepo root scripts.
- config.js is the only orchestration-tier script with a documented exception to the spawn-only rule (it reads package config files directly). This exception has no automated test — a future refactor converting config.js to spawn-based delegation would silently break cross-package atomic config reads without failing any test. Add a test case to cli-e2e-tests that exercises a cross-package config read and asserts the result is atomically consistent (e.g. both rex and hench config values appear in a single invocation output).
- [call graph] 447 internal calls, 0 outgoing, 0 incoming (cohesion: 1, coupling: 0)

</insights>
