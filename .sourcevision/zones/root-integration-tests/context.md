# Root Integration Tests — Zone Context

<!-- Per-zone context generated by Sourcevision. -->

<zone>

Zone: Root Integration Tests (`root-integration-tests`)
Files: 2, Cohesion: 0.00, Coupling: 0.00
Risk: at-risk (score: 0.50)
Description: Root-level integration tests validating cross-package contracts and scheduler startup behavior at the orchestration tier boundary.
Lines: 1474

</zone>

<files>

tests/integration/cross-package-contracts.test.js (JavaScript, 1074 lines, test)
tests/integration/scheduler-startup.test.js (JavaScript, 400 lines, test)

</files>

<findings>

[observation] [info] Isolated files — no import edges between 2 files, cohesion is unmeasurable (reported as 0)
[observation] [info] Only 2 files in this zone; if additional root-level integration tests are added, they should land here rather than in the e2e zone to keep integration vs. e2e test categories distinct.
[observation] [info] cross-package-contracts.test.js is a high-value architectural guard — it should be run in CI before any merge that touches gateway modules or package public APIs.
[suggestion] [info] Zone "Root Integration Tests" (root-integration-tests) has at-risk risk (score: 0.50, cohesion: 0.00, coupling: 0.00) — unreliable: zone has only 2 files (minimum 5 for reliable metrics)

</findings>

<insights>

- Isolated files — no import edges between 2 files, cohesion is unmeasurable (reported as 0)
- Zero cohesion and zero coupling are expected — these two test files each test distinct contracts (cross-package API surface vs. scheduler boot) and do not import each other.
- The algorithmicId 'logo' is a Louvain artifact with no semantic meaning; the files are clearly integration tests and should carry the '-tests' suffix since the zone contains only test files.
- cross-package-contracts.test.js validates gateway and tier-boundary invariants that are load-bearing for the architecture; any regression here signals a structural violation rather than a feature bug.
- Only 2 files in this zone; if additional root-level integration tests are added, they should land here rather than in the e2e zone to keep integration vs. e2e test categories distinct.
- cross-package-contracts.test.js is a high-value architectural guard — it should be run in CI before any merge that touches gateway modules or package public APIs.
- [call graph] 25 internal calls, 0 outgoing, 0 incoming (cohesion: 1, coupling: 0)

</insights>
