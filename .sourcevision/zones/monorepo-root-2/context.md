# Monorepo Root — Zone Context

<!-- Per-zone context generated by Sourcevision. -->

<zone>

Zone: Monorepo Root (`monorepo-root-2`)
Files: 2, Cohesion: 0.00, Coupling: 0.00
Risk: at-risk (score: 0.50)
Description: Project-level configuration, orchestration entry points, and cross-package tooling scripts that form the n-dx CLI surface.
Lines: 510

</zone>

<files>

packages/web/src/landing/landing.ts (TypeScript, 351 lines, source)
packages/web/tests/unit/landing/landing.test.ts (TypeScript, 159 lines, test)

</files>

<imports>

Internal:
  packages/web/tests/unit/landing/landing.test.ts → packages/web/src/landing/landing.ts {*}

</imports>

<findings>

[observation] [info] Isolated files — no import edges between 2 files, cohesion is unmeasurable (reported as 0)
[suggestion] [info] Zone "Monorepo Root" (monorepo-root-2) has at-risk risk (score: 0.50, cohesion: 0.00, coupling: 0.00) — unreliable: zone has only 2 files (minimum 5 for reliable metrics)

</findings>

<insights>

- Isolated files — no import edges between 2 files, cohesion is unmeasurable (reported as 0)
- Zero cohesion and zero coupling confirm this is a catch-all residual zone; the files belong to at least three distinct concerns (production landing page, test infrastructure, analysis scripts) and should be re-homed.
- landing.ts is a production source file and must not be treated as test infrastructure — it should be pinned to the web-dashboard zone so the '-tests' naming convention is not inadvertently applied.
- The two analysis scripts (hench-callgraph-analysis.mjs, parse-gateway-exports.mjs) are developer tooling with no package affiliation; pinning them to the monorepo-root zone would surface them in the correct architectural layer.
- Zone cohesion is 0 — this is a residual community with no shared import edges; dissolving it via zone pins would eliminate misleading health metrics.
- landing.ts is a production source file co-located with test files in the same algorithmic community, which risks misclassifying production code as test-only if zone naming is applied naively.
- The two .mjs analysis scripts are monorepo-level developer utilities and would be better homed alongside other root-level tooling rather than in a web-package zone.
- [call graph] 23 internal calls, 0 outgoing, 0 incoming (cohesion: 1, coupling: 0)

</insights>
