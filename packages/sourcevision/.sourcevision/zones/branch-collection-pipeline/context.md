# Branch Collection Pipeline — Zone Context

<!-- Per-zone context generated by Sourcevision. -->

<zone>

Zone: Branch Collection Pipeline (`branch-collection-pipeline`)
Files: 5, Cohesion: 0.20, Coupling: 0.80
Risk: catastrophic (score: 0.80)
Description: Orchestrates branch-work data collection and exposes it through the PR-markdown CLI command.
Entry points: src/cli/commands/pr-markdown.ts
Lines: 2005

</zone>

<files>

src/analyzers/branch-work-collector.ts (TypeScript, 418 lines, source)
src/cli/commands/pr-markdown.ts (TypeScript, 158 lines, source)
tests/integration/pr-markdown-reviewer-output.test.ts (TypeScript, 157 lines, test)
tests/unit/analyzers/branch-work-collector.test.ts (TypeScript, 591 lines, test)
tests/unit/cli/pr-markdown.test.ts (TypeScript, 681 lines, test)

</files>

<imports>

Internal:
  src/cli/commands/pr-markdown.ts → src/analyzers/branch-work-collector.ts {collectBranchWork}
  src/cli/commands/pr-markdown.ts → src/analyzers/branch-work-collector.ts {BranchWorkResult}
  tests/integration/pr-markdown-reviewer-output.test.ts → src/cli/commands/pr-markdown.ts {cmdPrMarkdown}
  tests/unit/analyzers/branch-work-collector.test.ts → src/analyzers/branch-work-collector.ts {collectBranchWork, parsePRDDocument, diffCompletedItems, buildBranchWorkItems}
  tests/unit/analyzers/branch-work-collector.test.ts → src/analyzers/branch-work-collector.ts {BranchWorkResult, CollectorOptions}
  tests/unit/cli/pr-markdown.test.ts → src/analyzers/branch-work-collector.ts {BranchWorkResult}
  tests/unit/cli/pr-markdown.test.ts → src/cli/commands/pr-markdown.ts {cmdPrMarkdown, toBranchWorkRecord, generatePrMarkdownFile, PR_MARKDOWN_FILENAME}

Outgoing (this zone → other zones):
  → branch-risk-domain: src/cli/commands/pr-markdown.ts → src/analyzers/branch-work-classifier.ts; src/cli/commands/pr-markdown.ts → src/generators/pr-markdown-template.ts; src/cli/commands/pr-markdown.ts → src/schema/v1.ts
  → sourcevision-analysis-core: src/cli/commands/pr-markdown.ts → src/cli/commands/constants.ts; src/cli/commands/pr-markdown.ts → src/cli/errors.ts; src/cli/commands/pr-markdown.ts → src/cli/output.ts; tests/unit/cli/pr-markdown.test.ts → src/cli/errors.ts

Incoming (other zones → this zone):
  ← sourcevision-analysis-core: src/cli/commands/analyze.ts → src/cli/commands/pr-markdown.ts; src/cli/index.ts → src/cli/commands/pr-markdown.ts

</imports>

<findings>

[observation] [warning] High coupling (0.8) — 4 imports target "sourcevision-analysis-core"
[observation] [warning] Low cohesion (0.2) — files are loosely related, consider splitting this zone
[observation] [warning] Cohesion 0.20 and coupling 0.80 are at warning thresholds; as an orchestration-layer adapter this is acceptable if the zone deliberately wires collector → CLI, but document this intent to prevent accidental logic accumulation.
[observation] [info] The CLI command (pr-markdown.ts) and its domain service (branch-work-collector.ts) are correctly co-located here, keeping the CLI thin and the domain logic in unit; this is a positive structural pattern.
[suggestion] [info] Zone "branch-collection-pipeline" has files across 5 directories — consider consolidating under a dedicated directory
[suggestion] [warning] 5 files across 5 distinct directories with zero directory co-location — the zone is held together only by import proximity; evaluate dissolving it entirely by pinning branch-work-collector.ts to the domain zone it couples with and pr-markdown.ts to a cli-commands zone, rather than treating the glue layer as an architectural unit
[suggestion] [critical] Zone "Branch Collection Pipeline" (branch-collection-pipeline) has catastrophic risk (score: 0.80, cohesion: 0.20, coupling: 0.80) — requires immediate architectural intervention
[relationship] [warning] branch-collection-pipeline connects to the rest of the codebase exclusively via unit-2 as a single coupling point — this makes unit-2 a mandatory transitive dependency for any consumer of the collection pipeline, increasing blast radius if unit-2 is refactored.
[anti-pattern] [warning] CLI command (pr-markdown.ts) and domain service (branch-work-collector.ts) are in the same zone — split along the CLI/domain boundary so the domain service can be imported independently of CLI infrastructure
[anti-pattern] [info] tests/integration/pr-markdown-reviewer-output.test.ts validates markdown rendering output but is assigned to the collection pipeline zone — misassigned test obscures which production code it guards and should be co-located with the template code in branch-risk-domain

</findings>

<insights>

- Low cohesion (0.2) — files are loosely related, consider splitting this zone
- High coupling (0.8) — 4 imports target "sourcevision-analysis-core"
- The pairing of branch-work-collector.ts (a service) with pr-markdown.ts (a CLI command) in one zone reveals a thin pipeline: collect → render — making the data flow easy to trace
- Low cohesion (0.20) and high coupling (0.80) signal that this zone functions as a thin adapter/orchestration layer rather than an encapsulated module, which is architecturally valid but should be explicit
- Four imports flowing into unit-2 and three into unit suggest this zone is downstream of both core zones — confirm it never imports back into them to keep the DAG acyclic
- Cohesion 0.20 and coupling 0.80 are at warning thresholds; as an orchestration-layer adapter this is acceptable if the zone deliberately wires collector → CLI, but document this intent to prevent accidental logic accumulation.
- The CLI command (pr-markdown.ts) and its domain service (branch-work-collector.ts) are correctly co-located here, keeping the CLI thin and the domain logic in unit; this is a positive structural pattern.
- Zone "branch-collection-pipeline" has files across 5 directories — consider consolidating under a dedicated directory
- Only appears in cross-zone imports as 'unit-analyzers → unit-2' and 'unit-analyzers → unit', suggesting branch-collection-pipeline's outbound coupling flows entirely through unit-2 as an intermediary — it never directly touches unit or unit-cli, making it an indirect downstream consumer
- branch-collection-pipeline connects to the rest of the codebase exclusively via unit-2 as a single coupling point — this makes unit-2 a mandatory transitive dependency for any consumer of the collection pipeline, increasing blast radius if unit-2 is refactored.
- src/cli/commands/pr-markdown.ts (CLI layer) and branch-work-collector.ts (domain service) occupy the same zone — CLI commands are leaf consumers and domain services are reusable logic; their co-location prevents the domain service from being used without also loading CLI machinery
- tests/integration/pr-markdown-reviewer-output.test.ts tests markdown rendering output, which is a presentation concern owned by pr-markdown-template.ts in branch-risk-domain — the test is misassigned to the collection pipeline zone rather than to the template zone it actually exercises
- CLI command (pr-markdown.ts) and domain service (branch-work-collector.ts) are in the same zone — split along the CLI/domain boundary so the domain service can be imported independently of CLI infrastructure
- tests/integration/pr-markdown-reviewer-output.test.ts validates markdown rendering output but is assigned to the collection pipeline zone — misassigned test obscures which production code it guards and should be co-located with the template code in branch-risk-domain
- Every file in this zone lives in a different directory: src/analyzers/, src/cli/commands/, tests/integration/, tests/unit/analyzers/, tests/unit/cli/ — the zone has zero directory co-location (5 files, 5 directories), meaning import-graph proximity is the sole binding force; directory-level navigation provides no indication that these files form a coherent unit
- 5 files across 5 distinct directories with zero directory co-location — the zone is held together only by import proximity; evaluate dissolving it entirely by pinning branch-work-collector.ts to the domain zone it couples with and pr-markdown.ts to a cli-commands zone, rather than treating the glue layer as an architectural unit
- [call graph] 213 internal calls, 5 outgoing, 2 incoming (cohesion: 0.98, coupling: 0.02)

</insights>
