# PRD Structural Validation — Zone Context

<!-- Per-zone context generated by Sourcevision. -->

<zone>

Zone: PRD Structural Validation (`prd-structural-validation`)
Files: 11, Cohesion: 0.36, Coupling: 0.64
Risk: critical (score: 0.64)
Description: Implements PRD integrity checks including structural consistency, epic correlation, and interactive fix prompts surfaced through the validate CLI commands.
Entry points: src/cli/commands/validate.ts, src/core/structural.ts
Lines: 4935

</zone>

<files>

src/cli/commands/validate-interactive.ts (TypeScript, 344 lines, source)
src/cli/commands/validate.ts (TypeScript, 266 lines, source)
src/core/epic-correlation.ts (TypeScript, 279 lines, source)
src/core/structural.ts (TypeScript, 377 lines, source)
tests/integration/smart-add-orphaned-parent.test.ts (TypeScript, 330 lines, test)
tests/unit/cli/commands/validate-epicless.test.ts (TypeScript, 280 lines, test)
tests/unit/cli/commands/validate-interactive.test.ts (TypeScript, 856 lines, test)
tests/unit/cli/commands/validate.test.ts (TypeScript, 594 lines, test)
tests/unit/core/epic-correlation.test.ts (TypeScript, 531 lines, test)
tests/unit/core/epicless.test.ts (TypeScript, 196 lines, test)
tests/unit/core/structural.test.ts (TypeScript, 882 lines, test)

</files>

<imports>

Internal:
  src/cli/commands/validate-interactive.ts → src/core/epic-correlation.ts {correlateEpiclessFeatures, formatScore}
  src/cli/commands/validate-interactive.ts → src/core/epic-correlation.ts {CorrelationResult}
  src/cli/commands/validate-interactive.ts → src/core/structural.ts {EpiclessFeature}
  src/cli/commands/validate.ts → src/cli/commands/validate-interactive.ts {resolveEpiclessFeatures, applyEpiclessResolutions}
  src/cli/commands/validate.ts → src/cli/commands/validate-interactive.ts {PromptFn}
  src/cli/commands/validate.ts → src/core/structural.ts {validateStructure, findEpiclessFeatures}
  src/core/epic-correlation.ts → src/core/structural.ts {EpiclessFeature}
  tests/integration/smart-add-orphaned-parent.test.ts → src/cli/commands/validate.ts {cmdValidate}
  tests/integration/smart-add-orphaned-parent.test.ts → src/core/structural.ts {validateStructure}
  tests/unit/cli/commands/validate-epicless.test.ts → src/cli/commands/validate.ts {cmdValidate}
  tests/unit/cli/commands/validate-interactive.test.ts → src/cli/commands/validate-interactive.ts {resolveEpiclessFeatures, applyEpiclessResolutions}
  tests/unit/cli/commands/validate-interactive.test.ts → src/cli/commands/validate-interactive.ts {EpiclessResolution, PromptFn}
  tests/unit/cli/commands/validate-interactive.test.ts → src/core/structural.ts {EpiclessFeature}
  tests/unit/cli/commands/validate.test.ts → src/cli/commands/validate.ts {cmdValidate}
  tests/unit/core/epic-correlation.test.ts → src/core/epic-correlation.ts {computeTagOverlap, computeCorrelationSignals, computeCombinedScore, rankEpicsForFeature, correlateEpiclessFeatures, formatScore}
  tests/unit/core/epic-correlation.test.ts → src/core/structural.ts {EpiclessFeature}
  tests/unit/core/epicless.test.ts → src/core/structural.ts {findEpiclessFeatures}
  tests/unit/core/structural.test.ts → src/core/structural.ts {validateStructure}

Outgoing (this zone → other zones):
  → cli-mcp-interface: src/cli/commands/validate.ts → src/cli/commands/constants.ts; tests/integration/smart-add-orphaned-parent.test.ts → src/cli/commands/constants.ts; tests/integration/smart-add-orphaned-parent.test.ts → src/cli/commands/init.ts; tests/integration/smart-add-orphaned-parent.test.ts → src/cli/commands/smart-add.ts
  → integration-adapter-layer: src/cli/commands/validate.ts → src/store/index.ts; tests/integration/smart-add-orphaned-parent.test.ts → src/store/index.ts
  → llm-analysis-engine: tests/integration/smart-add-orphaned-parent.test.ts → src/analyze/index.ts
  → prd-domain-operations: src/cli/commands/validate-interactive.ts → src/core/remove-feature.ts; src/cli/commands/validate-interactive.ts → src/schema/index.ts; src/cli/commands/validate-interactive.ts → src/schema/index.ts; src/cli/commands/validate.ts → src/schema/index.ts; src/cli/commands/validate.ts → src/schema/index.ts; src/core/epic-correlation.ts → src/core/keywords.ts; src/core/epic-correlation.ts → src/schema/index.ts; src/core/epic-correlation.ts → src/schema/index.ts; src/core/structural.ts → src/schema/index.ts; src/core/structural.ts → src/schema/index.ts; tests/integration/smart-add-orphaned-parent.test.ts → src/schema/index.ts; tests/unit/cli/commands/validate-epicless.test.ts → src/schema/index.ts; tests/unit/cli/commands/validate-interactive.test.ts → src/schema/index.ts; tests/unit/cli/commands/validate.test.ts → src/schema/index.ts; tests/unit/core/epic-correlation.test.ts → src/schema/index.ts; tests/unit/core/epicless.test.ts → src/schema/index.ts; tests/unit/core/structural.test.ts → src/schema/index.ts
  → prd-proposal-pipeline: src/core/epic-correlation.ts → src/analyze/dedupe.ts
  → rex-cli-command-layer: src/cli/commands/validate-interactive.ts → src/cli/output.ts; src/cli/commands/validate.ts → src/cli/output.ts
  → schema-storage-reporting: src/cli/commands/validate.ts → src/schema/validate.ts
  → tree-dag-primitives: src/cli/commands/validate-interactive.ts → src/core/tree.ts; src/cli/commands/validate.ts → src/core/dag.ts; src/core/structural.ts → src/core/tree.ts

Incoming (other zones → this zone):
  ← prd-domain-operations: src/core/reorganize.ts → src/core/structural.ts
  ← rex-cli-command-layer: src/cli/index.ts → src/cli/commands/validate.ts
  ← schema-storage-reporting: src/cli/commands/report.ts → src/core/structural.ts

</imports>

<findings>

[observation] [warning] High coupling (0.64) — 17 imports target "prd-domain-operations"
[observation] [warning] Low cohesion (0.36) — files are loosely related, consider splitting this zone
[observation] [warning] Cohesion 0.36 and coupling 0.64 both exceed warning thresholds; the zone conflates core utility files, CLI command handlers, and test files into one community.
[observation] [info] validate → core carries 17 imports, confirming that validation heavily depends on domain primitives; if epic-correlation.ts and structural.ts moved to the core zone this coupling score would drop significantly.
[suggestion] [info] Zone "prd-structural-validation" has files across 5 directories — consider consolidating under a dedicated directory
[suggestion] [info] Extract terminal I/O from validate-interactive.ts into a validate-presenter.ts module to create a seam between validation rules and presentation — rules become unit-testable without TTY and the presentation layer can be swapped for future non-interactive modes
[suggestion] [warning] RISK AREA: cohesion 0.36 + coupling 0.64 both at warning thresholds — the zone simultaneously owns core utilities (epic-correlation, structural), CLI command handlers (validate, validate-interactive), and three unit test files; a single change to the validate command interface requires updating files from all three categories
[suggestion] [warning] Zone "PRD Structural Validation" (prd-structural-validation) has critical risk (score: 0.64, cohesion: 0.36, coupling: 0.64) — requires refactoring before new feature development
[relationship] [warning] epic-correlation.ts and structural.ts appear to originate in prd-structural-validation but flow 17 imports upward into prd-domain-operations; a utility that is more heavily consumed by core than by its own zone has inverted dependency direction — moving these files to core would align the import arrow with the architectural hierarchy.
[anti-pattern] [warning] One integration test (smart-add-orphaned-parent.test.ts) is stranded in the structural-validation zone while four other integration tests in the same directory have no zone assignment — the integration test layer lacks a dedicated zone, making cross-integration-test dependencies invisible to zone health tooling
[anti-pattern] [warning] epic-correlation.ts imports from src/analyze/dedupe.js — a core utility file pulling in an analyze-subsystem dependency creates a hidden transitive coupling path from validation → core → analyze that bypasses zone boundary enforcement

</findings>

<insights>

- Low cohesion (0.36) — files are loosely related, consider splitting this zone
- High coupling (0.64) — 17 imports target "prd-domain-operations"
- epic-correlation.ts and structural.ts are general-purpose core utilities that appear in this zone due to test co-location — evaluate whether they belong in the core PRD domain zone instead
- The interactive validate path (validate-interactive.ts) combines user-facing I/O with validation logic; splitting presentation from validation rules would make the rules unit-testable in isolation
- Three validate test files land here alongside production code, which inflates the zone's apparent scope — the test gravity is a signal that validate logic is tightly coupled to its own tests rather than shared domain infrastructure
- Cohesion 0.36 and coupling 0.64 both exceed warning thresholds; the zone conflates core utility files, CLI command handlers, and test files into one community.
- validate → core carries 17 imports, confirming that validation heavily depends on domain primitives; if epic-correlation.ts and structural.ts moved to the core zone this coupling score would drop significantly.
- Zone "prd-structural-validation" has files across 5 directories — consider consolidating under a dedicated directory
- The 17 validate→core imports with only 1 reverse import suggests epic-correlation.ts and structural.ts are defined inside the validate zone but heavily referenced back toward core consumers — if these utilities were pinned to core the coupling arrow would reverse direction, which is the correct orientation for domain utilities.
- epic-correlation.ts and structural.ts appear to originate in prd-structural-validation but flow 17 imports upward into prd-domain-operations; a utility that is more heavily consumed by core than by its own zone has inverted dependency direction — moving these files to core would align the import arrow with the architectural hierarchy.
- tests/integration/smart-add-orphaned-parent.test.ts lands in this zone while all other integration tests live in tests/integration/ without a dedicated zone — the integration test directory has 5 files but no cohesive zone, meaning integration test coverage is invisible to zone health reporting
- epic-correlation.ts imports from ../analyze/dedupe.js, creating a transitive dependency from the validation zone into the analyze subsystem that is invisible in the direct zone-coupling metrics — the validate→core coupling count understates the true fan-out
- epic-correlation.ts imports from src/analyze/dedupe.js — a core utility file pulling in an analyze-subsystem dependency creates a hidden transitive coupling path from validation → core → analyze that bypasses zone boundary enforcement
- One integration test (smart-add-orphaned-parent.test.ts) is stranded in the structural-validation zone while four other integration tests in the same directory have no zone assignment — the integration test layer lacks a dedicated zone, making cross-integration-test dependencies invisible to zone health tooling
- validate-interactive.ts mixes terminal I/O (readline prompts) with validation orchestration in a single file — the interactive presentation logic can change independently of the validation rules, but there is no seam between the two; extracting a validate-presenter.ts for I/O would make validate rules unit-testable without a TTY
- The three unit test files (validate.test.ts, validate-interactive.test.ts, validate-epicless.test.ts) test three distinct behaviors but live in a single zone alongside production code — there is no test file for epic-correlation.ts or structural.ts, meaning the most-coupled utilities in the zone have no dedicated tests visible in zone health reporting
- Extract terminal I/O from validate-interactive.ts into a validate-presenter.ts module to create a seam between validation rules and presentation — rules become unit-testable without TTY and the presentation layer can be swapped for future non-interactive modes
- RISK AREA: cohesion 0.36 + coupling 0.64 both at warning thresholds — the zone simultaneously owns core utilities (epic-correlation, structural), CLI command handlers (validate, validate-interactive), and three unit test files; a single change to the validate command interface requires updating files from all three categories
- [call graph] 466 internal calls, 75 outgoing, 3 incoming (cohesion: 0.86, coupling: 0.14)

</insights>
