# PRD Validation — Zone Context

<!-- Per-zone context generated by Sourcevision. -->

<zone>

Zone: PRD Validation (`prd-validation`)
Files: 11, Cohesion: 0.34, Coupling: 0.66
Risk: critical (score: 0.66)
Description: Structural integrity checks for the PRD — validate commands, epic-correlation heuristics, and interactive fix prompts — co-located with their unit and integration tests.
Entry points: src/cli/commands/validate.ts, src/core/structural.ts
Lines: 4935

</zone>

<files>

src/cli/commands/validate-interactive.ts (TypeScript, 344 lines, source)
src/cli/commands/validate.ts (TypeScript, 266 lines, source)
src/core/epic-correlation.ts (TypeScript, 279 lines, source)
src/core/structural.ts (TypeScript, 377 lines, source)
tests/integration/smart-add-orphaned-parent.test.ts (TypeScript, 330 lines, test)
tests/unit/cli/commands/validate-epicless.test.ts (TypeScript, 280 lines, test)
tests/unit/cli/commands/validate-interactive.test.ts (TypeScript, 856 lines, test)
tests/unit/cli/commands/validate.test.ts (TypeScript, 594 lines, test)
tests/unit/core/epic-correlation.test.ts (TypeScript, 531 lines, test)
tests/unit/core/epicless.test.ts (TypeScript, 196 lines, test)
tests/unit/core/structural.test.ts (TypeScript, 882 lines, test)

</files>

<imports>

Internal:
  src/cli/commands/validate-interactive.ts → src/core/epic-correlation.ts {correlateEpiclessFeatures, formatScore}
  src/cli/commands/validate-interactive.ts → src/core/epic-correlation.ts {CorrelationResult}
  src/cli/commands/validate-interactive.ts → src/core/structural.ts {EpiclessFeature}
  src/cli/commands/validate.ts → src/cli/commands/validate-interactive.ts {resolveEpiclessFeatures, applyEpiclessResolutions}
  src/cli/commands/validate.ts → src/cli/commands/validate-interactive.ts {PromptFn}
  src/cli/commands/validate.ts → src/core/structural.ts {validateStructure, findEpiclessFeatures}
  src/core/epic-correlation.ts → src/core/structural.ts {EpiclessFeature}
  tests/integration/smart-add-orphaned-parent.test.ts → src/cli/commands/validate.ts {cmdValidate}
  tests/integration/smart-add-orphaned-parent.test.ts → src/core/structural.ts {validateStructure}
  tests/unit/cli/commands/validate-epicless.test.ts → src/cli/commands/validate.ts {cmdValidate}
  tests/unit/cli/commands/validate-interactive.test.ts → src/cli/commands/validate-interactive.ts {resolveEpiclessFeatures, applyEpiclessResolutions}
  tests/unit/cli/commands/validate-interactive.test.ts → src/cli/commands/validate-interactive.ts {EpiclessResolution, PromptFn}
  tests/unit/cli/commands/validate-interactive.test.ts → src/core/structural.ts {EpiclessFeature}
  tests/unit/cli/commands/validate.test.ts → src/cli/commands/validate.ts {cmdValidate}
  tests/unit/core/epic-correlation.test.ts → src/core/epic-correlation.ts {computeTagOverlap, computeCorrelationSignals, computeCombinedScore, rankEpicsForFeature, correlateEpiclessFeatures, formatScore}
  tests/unit/core/epic-correlation.test.ts → src/core/structural.ts {EpiclessFeature}
  tests/unit/core/epicless.test.ts → src/core/structural.ts {findEpiclessFeatures}
  tests/unit/core/structural.test.ts → src/core/structural.ts {validateStructure}

Outgoing (this zone → other zones):
  → cli-command-layer: src/cli/commands/validate-interactive.ts → src/cli/output.ts; src/cli/commands/validate.ts → src/cli/output.ts
  → prd-analysis-pipeline: src/core/epic-correlation.ts → src/analyze/dedupe.ts; tests/integration/smart-add-orphaned-parent.test.ts → src/analyze/index.ts; tests/integration/smart-add-orphaned-parent.test.ts → src/cli/commands/smart-add.ts
  → prd-store-sync: src/cli/commands/validate-interactive.ts → src/schema/index.ts; src/cli/commands/validate-interactive.ts → src/schema/index.ts; src/cli/commands/validate.ts → src/schema/index.ts; src/cli/commands/validate.ts → src/schema/index.ts; src/cli/commands/validate.ts → src/schema/validate.ts; src/cli/commands/validate.ts → src/store/index.ts; src/core/epic-correlation.ts → src/schema/index.ts; src/core/epic-correlation.ts → src/schema/index.ts; src/core/structural.ts → src/schema/index.ts; src/core/structural.ts → src/schema/index.ts; tests/integration/smart-add-orphaned-parent.test.ts → src/cli/commands/init.ts; tests/integration/smart-add-orphaned-parent.test.ts → src/schema/index.ts; tests/integration/smart-add-orphaned-parent.test.ts → src/store/index.ts; tests/unit/cli/commands/validate-epicless.test.ts → src/schema/index.ts; tests/unit/cli/commands/validate-interactive.test.ts → src/schema/index.ts; tests/unit/cli/commands/validate.test.ts → src/schema/index.ts; tests/unit/core/epic-correlation.test.ts → src/schema/index.ts; tests/unit/core/epicless.test.ts → src/schema/index.ts; tests/unit/core/structural.test.ts → src/schema/index.ts
  → prd-tree-operations: src/cli/commands/validate-interactive.ts → src/core/remove-feature.ts; src/cli/commands/validate-interactive.ts → src/core/tree.ts; src/cli/commands/validate.ts → src/cli/commands/constants.ts; src/cli/commands/validate.ts → src/core/dag.ts; src/core/epic-correlation.ts → src/core/keywords.ts; src/core/structural.ts → src/core/tree.ts; tests/integration/smart-add-orphaned-parent.test.ts → src/cli/commands/constants.ts

Incoming (other zones → this zone):
  ← cli-command-layer: src/cli/commands/status.ts → src/core/structural.ts; src/cli/index.ts → src/cli/commands/validate.ts
  ← prd-tree-operations: src/cli/commands/report.ts → src/core/structural.ts; src/core/reorganize.ts → src/core/structural.ts

</imports>

<findings>

[observation] [warning] High coupling (0.66) — 19 imports target "prd-store-sync"
[observation] [warning] Low cohesion (0.34) — files are loosely related, consider splitting this zone
[observation] [info] 19 imports from this zone into unit-store and 7 into unit-core indicate validation logic reaches across both major layers; consider whether structural checks belong in the store layer or as a validation pass in unit-core.
[observation] [warning] Cohesion of 0.34 and coupling of 0.66 exceed warning thresholds; epic-correlation and structural utilities appear to be widely imported utilities that have not yet found a stable home in the zone hierarchy.
[suggestion] [info] Zone "prd-validation" has files across 5 directories — consider consolidating under a dedicated directory
[suggestion] [warning] Dissolve prd-validation as a top-priority action: move epic-correlation.ts and structural.ts into prd-tree-operations (domain utilities); move validate.ts and validate-interactive.ts into the cli zone (CLI commands). This is a zero-logic-change refactor that eliminates the only doubly-failed zone in the codebase.
[suggestion] [info] Zone name 'prd-validation' is a misnomer for its actual contents. Even before dissolving, renaming would prevent future misassignment of CLI commands into this zone based on the misleading 'validation' label.
[suggestion] [warning] Zone "PRD Validation" (prd-validation) has critical risk (score: 0.66, cohesion: 0.34, coupling: 0.66) — requires refactoring before new feature development
[pattern] [warning] Test files in prd-validation (validate.test.ts, validate-interactive.test.ts, etc.) share a community with production files solely because of import proximity; this contaminates cohesion and coupling scores and should be resolved by moving tests to the unit zone or co-locating them with the unit-cli zone rather than letting them drift into a production-dominated zone
[anti-pattern] [warning] prd-validation contains 4 production files that belong to two different architectural layers: epic-correlation.ts and structural.ts are domain utilities (should join prd-tree-operations) while validate.ts and validate-interactive.ts are CLI commands (should join cli). The zone exists as an artifact of import proximity, not architectural intent.

</findings>

<insights>

- Low cohesion (0.34) — files are loosely related, consider splitting this zone
- High coupling (0.66) — 19 imports target "prd-store-sync"
- Low cohesion (0.34) and high coupling (0.66) are the weakest metrics in this batch; epic-correlation.ts and structural.ts are likely pulled into many consumers, inflating coupling
- The zone mixes production files (validate.ts, validate-interactive.ts, epic-correlation.ts, structural.ts) with tests — the test files likely follow the production modules by import proximity rather than intent
- Extracting epic-correlation.ts and structural.ts into prd-tree-operations or a dedicated analysis-support module could reduce coupling and raise cohesion
- Cohesion of 0.34 and coupling of 0.66 exceed warning thresholds; epic-correlation and structural utilities appear to be widely imported utilities that have not yet found a stable home in the zone hierarchy.
- 19 imports from this zone into unit-store and 7 into unit-core indicate validation logic reaches across both major layers; consider whether structural checks belong in the store layer or as a validation pass in unit-core.
- Zone "prd-validation" has files across 5 directories — consider consolidating under a dedicated directory
- Because prd-validation co-mingles test files with production files, its coupling metric of 0.66 is partially inflated by test-to-production imports; the true production coupling is unknowable without separating the two, making the metric misleading for architectural decisions
- Test files in prd-validation (validate.test.ts, validate-interactive.test.ts, etc.) share a community with production files solely because of import proximity; this contaminates cohesion and coupling scores and should be resolved by moving tests to the unit zone or co-locating them with the unit-cli zone rather than letting them drift into a production-dominated zone
- With only 4 production files, prd-validation is too small to justify independent zone status. epic-correlation.ts and structural.ts are domain utilities consumed across prd-tree-operations; validate.ts and validate-interactive.ts are CLI commands. Dissolving this zone into its natural hosts would eliminate the zone's low cohesion and high coupling without any logic changes.
- prd-validation contains 4 production files that belong to two different architectural layers: epic-correlation.ts and structural.ts are domain utilities (should join prd-tree-operations) while validate.ts and validate-interactive.ts are CLI commands (should join cli). The zone exists as an artifact of import proximity, not architectural intent.
- prd-validation is the only zone that simultaneously fails both the cohesion threshold (0.34 < 0.4) and the coupling threshold (0.66 > 0.6) — it is the sole zone in a doubly-failed state, making it the highest-priority dissolution target in the codebase.
- The zone name 'prd-validation' implies a domain layer but validate.ts and validate-interactive.ts are CLI commands — the name masks the architectural category of half its production files and will cause future CLI commands with 'validate' in their name to be incorrectly placed here.
- Dissolve prd-validation as a top-priority action: move epic-correlation.ts and structural.ts into prd-tree-operations (domain utilities); move validate.ts and validate-interactive.ts into the cli zone (CLI commands). This is a zero-logic-change refactor that eliminates the only doubly-failed zone in the codebase.
- Zone name 'prd-validation' is a misnomer for its actual contents. Even before dissolving, renaming would prevent future misassignment of CLI commands into this zone based on the misleading 'validation' label.
- [call graph] 466 internal calls, 74 outgoing, 3 incoming (cohesion: 0.86, coupling: 0.14)

</insights>
