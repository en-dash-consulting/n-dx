# Data Persistence & Validation — Zone Context

<!-- Per-zone context generated by Sourcevision. -->

<zone>

Zone: Data Persistence & Validation (`data-persistence-validation`)
Files: 8, Cohesion: 0.17, Coupling: 0.83
Risk: catastrophic (score: 0.83)
Description: The storage and integrity layer: file-based PRD adapter, schema validation, and the report command that surfaces store state.
Entry points: src/cli/commands/report.ts, src/schema/validate.ts, src/store/file-adapter.ts
Lines: 2789

</zone>

<files>

src/cli/commands/report.ts (TypeScript, 278 lines, source)
src/schema/validate.ts (TypeScript, 170 lines, source)
src/store/file-adapter.ts (TypeScript, 169 lines, source)
tests/e2e/cli-init.test.ts (TypeScript, 213 lines, test)
tests/unit/cli/commands/report.test.ts (TypeScript, 419 lines, test)
tests/unit/schema/validate.test.ts (TypeScript, 628 lines, test)
tests/unit/store/file-adapter.test.ts (TypeScript, 265 lines, test)
tests/unit/store/store-contract.test.ts (TypeScript, 647 lines, test)

</files>

<imports>

Internal:
  src/cli/commands/report.ts → src/schema/validate.ts {validateDocument, validateConfig}
  src/store/file-adapter.ts → src/schema/validate.ts {validateDocument, validateConfig, validateLogEntry}
  tests/e2e/cli-init.test.ts → src/schema/validate.ts {RexConfigSchema, PRDDocumentSchema}
  tests/unit/cli/commands/report.test.ts → src/cli/commands/report.ts {cmdReport}
  tests/unit/schema/validate.test.ts → src/schema/validate.ts {validateDocument, validateConfig, validateLogEntry, formatValidationErrors}
  tests/unit/store/file-adapter.test.ts → src/store/file-adapter.ts {FileStore, ensureRexDir}
  tests/unit/store/store-contract.test.ts → src/store/file-adapter.ts {FileStore, ensureRexDir}

Outgoing (this zone → other zones):
  → mutation-commands: src/cli/commands/report.ts → src/cli/output.ts
  → notion-integration: tests/unit/store/store-contract.test.ts → src/store/notion-adapter.ts
  → prd-analysis-pipeline: src/cli/commands/report.ts → src/core/structural.ts; src/store/file-adapter.ts → src/store/atomic-write.ts; src/store/file-adapter.ts → src/store/project-config.ts
  → prd-operations-core: src/cli/commands/report.ts → src/schema/index.ts; src/cli/commands/report.ts → src/schema/index.ts; src/schema/validate.ts → src/schema/v1.ts; src/store/file-adapter.ts → src/schema/index.ts; src/store/file-adapter.ts → src/store/contracts.ts; tests/e2e/cli-init.test.ts → src/schema/v1.ts; tests/unit/cli/commands/report.test.ts → src/schema/index.ts; tests/unit/store/file-adapter.test.ts → src/schema/index.ts; tests/unit/store/file-adapter.test.ts → src/schema/index.ts; tests/unit/store/store-contract.test.ts → src/schema/index.ts; tests/unit/store/store-contract.test.ts → src/schema/index.ts; tests/unit/store/store-contract.test.ts → src/store/contracts.ts
  → remote-integration: src/cli/commands/report.ts → src/core/dag.ts; src/cli/commands/report.ts → src/core/tree.ts; src/store/file-adapter.ts → src/core/canonical.ts; src/store/file-adapter.ts → src/core/tree.ts; tests/unit/store/file-adapter.test.ts → src/core/canonical.ts; tests/unit/store/store-contract.test.ts → src/core/canonical.ts; tests/unit/store/store-contract.test.ts → src/store/notion-client.ts
  → rex-status-mcp-cli: src/cli/commands/report.ts → src/cli/commands/constants.ts; src/cli/commands/report.ts → src/core/stats.ts; src/cli/commands/report.ts → src/core/stats.ts

Incoming (other zones → this zone):
  ← mutation-commands: src/cli/index.ts → src/cli/commands/report.ts
  ← notion-integration: src/store/notion-adapter.ts → src/schema/validate.ts
  ← prd-analysis-pipeline: src/cli/commands/validate.ts → src/schema/validate.ts
  ← prd-operations-core: tests/unit/core/requirements.test.ts → src/schema/validate.ts; tests/unit/schema/schema-version.test.ts → src/schema/validate.ts
  ← remote-integration: src/store/adapter-registry.ts → src/store/file-adapter.ts; src/store/index.ts → src/store/file-adapter.ts; src/store/index.ts → src/store/file-adapter.ts; tests/unit/cli/commands/sync.test.ts → src/store/file-adapter.ts; tests/unit/store/adapter-registry.test.ts → src/store/file-adapter.ts; tests/unit/store/resolve-store.test.ts → src/store/file-adapter.ts

</imports>

<findings>

[observation] [warning] High coupling (0.83) — 12 imports target "prd-operations-core"
[observation] [warning] Low cohesion (0.17) — files are loosely related, consider splitting this zone
[observation] [warning] Cohesion 0.17 and coupling 0.83 are flag-level metrics, but inspection of the three production files (file-adapter.ts, validate.ts, report.ts) shows they form a coherent persistence/integrity triad; the metrics are dominated by test cross-imports.
[observation] [info] cli-init.test.ts (an e2e test) is grouped in this zone alongside unit tests — confirm it belongs here semantically or whether it should migrate to the e2e zone (which already has cohesion 1.0).
[observation] [info] store-contract.test.ts validates the contract boundary between store/contracts.ts (in core) and file-adapter.ts; this test is high-value for catching regressions when the store interface evolves.
[suggestion] [info] Zone "data-persistence-validation" has files across 7 directories — consider consolidating under a dedicated directory
[suggestion] [warning] report.ts is a CLI display command ([cli-command] archetype) whose natural peer group is status.ts, usage.ts, and health.ts in rex-status-mcp-cli, not the persistence/validation files in this zone — move report.ts to the display zone and rename data-persistence-validation to 'persistence-validation' to accurately reflect the remaining files' shared responsibility
[suggestion] [critical] Zone "Data Persistence & Validation" (data-persistence-validation) has catastrophic risk (score: 0.83, cohesion: 0.17, coupling: 0.83) — requires immediate architectural intervention
[relationship] [warning] cli-init.test.ts belongs semantically and structurally in the e2e zone (cohesion 1.0, coupling 0) — its presence here dilutes e2e zone purity and inflates data-persistence-validation coupling; this is a misplacement, not a design decision
[anti-pattern] [warning] store/index.ts violates the project's explicit gateway rule ('gateways re-export; they contain no logic') by implementing 5 factory functions (createStore, createStoreWithConfig, createNotionStore, resolveStore, resolveRemoteStore) inline. Factory logic should be extracted to src/store/factory.ts with store/index.ts becoming a pure re-export barrel as specified in CLAUDE.md.

</findings>

<insights>

- Low cohesion (0.17) — files are loosely related, consider splitting this zone
- High coupling (0.83) — 12 imports target "prd-operations-core"
- file-adapter.ts, validate.ts, and report.ts form a natural read/validate/display triad — the zone grouping reflects real structural cohesion even if the metric says otherwise
- Cohesion 0.17 is misleading here: the low score is driven by test files importing from distant zones; the three production files are tightly related
- store-contract.test.ts existing alongside file-adapter.test.ts suggests an explicit contract layer — verify that contracts.ts in the core zone is the canonical definition and that these tests exercise it end-to-end
- Cohesion 0.17 and coupling 0.83 are flag-level metrics, but inspection of the three production files (file-adapter.ts, validate.ts, report.ts) shows they form a coherent persistence/integrity triad; the metrics are dominated by test cross-imports.
- cli-init.test.ts (an e2e test) is grouped in this zone alongside unit tests — confirm it belongs here semantically or whether it should migrate to the e2e zone (which already has cohesion 1.0).
- store-contract.test.ts validates the contract boundary between store/contracts.ts (in core) and file-adapter.ts; this test is high-value for catching regressions when the store interface evolves.
- Zone "data-persistence-validation" has files across 7 directories — consider consolidating under a dedicated directory
- cli-init.test.ts (an e2e test) residing in this zone is the only file breaking the e2e zone's perfect isolation (cohesion 1.0, coupling 0); migrating it would both restore e2e zone purity and reduce this zone's artificially inflated cross-zone coupling metric
- cli-init.test.ts belongs semantically and structurally in the e2e zone (cohesion 1.0, coupling 0) — its presence here dilutes e2e zone purity and inflates data-persistence-validation coupling; this is a misplacement, not a design decision
- store/index.ts re-exports from 8 source modules AND defines 5 factory functions (createStore, createStoreWithConfig, createNotionStore, resolveStore, resolveRemoteStore) — per the project's gateway rules (CLAUDE.md: 'Re-export only — gateways re-export; they contain no logic'), factory functions with implementation logic should not live in a gateway/barrel file. The factory logic belongs in a dedicated store-factory module that store/index.ts then re-exports
- store/index.ts violates the project's explicit gateway rule ('gateways re-export; they contain no logic') by implementing 5 factory functions (createStore, createStoreWithConfig, createNotionStore, resolveStore, resolveRemoteStore) inline. Factory logic should be extracted to src/store/factory.ts with store/index.ts becoming a pure re-export barrel as specified in CLAUDE.md.
- Zone name 'data-persistence-validation' accurately describes file-adapter.ts and validate.ts but misrepresents report.ts, which is a CLI rendering command with no persistence or validation responsibility — the zone name creates a false expectation that report.ts belongs to the data layer, when its natural peer group is status.ts and usage.ts in rex-status-mcp-cli
- report.ts (278 lines, in data-persistence-validation) and status-sections.ts (in rex-status-mcp-cli) are both CLI display commands that render PRD data — they share the same archetype and change cadence but are separated across zones, meaning a display-layer refactor must touch two zones simultaneously
- report.ts is a CLI display command ([cli-command] archetype) whose natural peer group is status.ts, usage.ts, and health.ts in rex-status-mcp-cli, not the persistence/validation files in this zone — move report.ts to the display zone and rename data-persistence-validation to 'persistence-validation' to accurately reflect the remaining files' shared responsibility
- [call graph] 149 internal calls, 20 outgoing, 21 incoming (cohesion: 0.88, coupling: 0.12)

</insights>
