# Schema Storage and Reporting — Zone Context

<!-- Per-zone context generated by Sourcevision. -->

<zone>

Zone: Schema Storage and Reporting (`schema-storage-reporting`)
Files: 7, Cohesion: 0.17, Coupling: 0.83
Risk: catastrophic (score: 0.83)
Description: Groups schema validation, the file-system PRD store adapter, and the report command — three loosely related infrastructure concerns that share no strong internal dependency.
Entry points: src/cli/commands/report.ts, src/schema/validate.ts, src/store/file-adapter.ts
Lines: 2144

</zone>

<files>

src/cli/commands/report.ts (TypeScript, 278 lines, source)
src/schema/validate.ts (TypeScript, 172 lines, source)
src/store/file-adapter.ts (TypeScript, 169 lines, source)
tests/e2e/cli-init.test.ts (TypeScript, 213 lines, test)
tests/unit/cli/commands/report.test.ts (TypeScript, 419 lines, test)
tests/unit/schema/validate.test.ts (TypeScript, 628 lines, test)
tests/unit/store/file-adapter.test.ts (TypeScript, 265 lines, test)

</files>

<imports>

Internal:
  src/cli/commands/report.ts → src/schema/validate.ts {validateDocument, validateConfig}
  src/store/file-adapter.ts → src/schema/validate.ts {validateDocument, validateConfig, validateLogEntry}
  tests/e2e/cli-init.test.ts → src/schema/validate.ts {RexConfigSchema, PRDDocumentSchema}
  tests/unit/cli/commands/report.test.ts → src/cli/commands/report.ts {cmdReport}
  tests/unit/schema/validate.test.ts → src/schema/validate.ts {validateDocument, validateConfig, validateLogEntry, formatValidationErrors}
  tests/unit/store/file-adapter.test.ts → src/store/file-adapter.ts {FileStore, ensureRexDir}

Outgoing (this zone → other zones):
  → cli-mcp-interface: src/cli/commands/report.ts → src/cli/commands/constants.ts
  → llm-analysis-engine: src/store/file-adapter.ts → src/store/atomic-write.ts; src/store/file-adapter.ts → src/store/project-config.ts
  → prd-domain-operations: src/cli/commands/report.ts → src/schema/index.ts; src/cli/commands/report.ts → src/schema/index.ts; src/schema/validate.ts → src/schema/v1.ts; src/store/file-adapter.ts → src/schema/index.ts; src/store/file-adapter.ts → src/store/contracts.ts; tests/e2e/cli-init.test.ts → src/schema/v1.ts; tests/unit/cli/commands/report.test.ts → src/schema/index.ts; tests/unit/store/file-adapter.test.ts → src/schema/index.ts; tests/unit/store/file-adapter.test.ts → src/schema/index.ts
  → prd-proposal-pipeline: src/store/file-adapter.ts → src/core/canonical.ts; tests/unit/store/file-adapter.test.ts → src/core/canonical.ts
  → prd-structural-validation: src/cli/commands/report.ts → src/core/structural.ts
  → rex-cli-command-layer: src/cli/commands/report.ts → src/cli/output.ts; src/cli/commands/report.ts → src/core/stats.ts; src/cli/commands/report.ts → src/core/stats.ts
  → tree-dag-primitives: src/cli/commands/report.ts → src/core/dag.ts; src/cli/commands/report.ts → src/core/tree.ts; src/store/file-adapter.ts → src/core/tree.ts

Incoming (other zones → this zone):
  ← integration-adapter-layer: src/store/adapter-registry.ts → src/store/file-adapter.ts; src/store/index.ts → src/store/file-adapter.ts; src/store/index.ts → src/store/file-adapter.ts; src/store/notion-adapter.ts → src/schema/validate.ts; tests/unit/cli/commands/sync.test.ts → src/store/file-adapter.ts; tests/unit/store/adapter-registry.test.ts → src/store/file-adapter.ts; tests/unit/store/resolve-store.test.ts → src/store/file-adapter.ts
  ← notion-integration-client: tests/unit/store/store-contract.test.ts → src/store/file-adapter.ts
  ← prd-domain-operations: tests/unit/core/requirements.test.ts → src/schema/validate.ts; tests/unit/schema/schema-version.test.ts → src/schema/validate.ts
  ← prd-structural-validation: src/cli/commands/validate.ts → src/schema/validate.ts
  ← rex-cli-command-layer: src/cli/index.ts → src/cli/commands/report.ts

</imports>

<findings>

[observation] [warning] High coupling (0.83) — 9 imports target "prd-domain-operations"
[observation] [warning] Low cohesion (0.17) — files are loosely related, consider splitting this zone
[observation] [warning] Cohesion of 0.17 is critically low and the three source files (report.ts, validate.ts, file-adapter.ts) serve distinct architectural roles — this zone is a detection artifact, not a real cohesion cluster, and should be dissolved by reassigning each file to its natural zone.
[observation] [info] Coupling of 0.83 confirms all three source files are highly dependent on external zones; once dissolved, the overall cross-zone import graph will be cleaner and these dependencies will be properly attributed to their owning zones.
[observation] [warning] The e2e test cli-init.test.ts appearing in this zone is a misclassification signal; e2e tests should be anchored to the zone under test (likely Project Configuration or CLI and MCP Interface), not grouped with schema/store unit tests.
[suggestion] [info] Zone "schema-storage-reporting" has files across 7 directories — consider consolidating under a dedicated directory
[suggestion] [warning] Dissolve this zone and pin each file to its natural home: report.ts → rex-cli-command-layer, validate.ts → prd-structural-validation, file-adapter.ts → integration-adapter-layer, e2e/cli-init.test.ts → e2e zone; the zone name itself signals it has no unifying domain concept
[suggestion] [critical] Zone "Schema Storage and Reporting" (schema-storage-reporting) has catastrophic risk (score: 0.83, cohesion: 0.17, coupling: 0.83) — requires immediate architectural intervention
[pattern] [warning] e2e/cli-init.test.ts was pulled into this zone because it imports both validate.ts and file-adapter.ts, demonstrating how e2e tests that import from multiple domain zones can contaminate zone assignments — zone detection should apply a blanket pin rule: tests/e2e/** always anchors to the e2e zone regardless of import neighbors
[relationship] [warning] schema-storage-reporting is the only zone where dissolution eliminates zero genuine cross-file relationships — all three source files have independent homes in existing zones and share no domain purpose; dissolving this zone would reduce overall zone count by one without losing any architectural signal

</findings>

<insights>

- Low cohesion (0.17) — files are loosely related, consider splitting this zone
- High coupling (0.83) — 9 imports target "prd-domain-operations"
- The grouping of file-adapter.ts, validate.ts, and report.ts by the community-detection algorithm likely reflects that all three are imported by tests in the same test subtree rather than genuine domain cohesion — treat this as a zone to dissolve rather than strengthen
- file-adapter.ts belongs conceptually in the store zone (algorithmicId: store) and validate.ts in the validate zone — their presence here suggests those zone boundaries were not fully resolved during the last analysis run
- report.ts is a CLI command with no obvious home; audit whether it belongs in rex-cli-command-layer alongside its peers
- Cohesion of 0.17 is critically low and the three source files (report.ts, validate.ts, file-adapter.ts) serve distinct architectural roles — this zone is a detection artifact, not a real cohesion cluster, and should be dissolved by reassigning each file to its natural zone.
- The e2e test cli-init.test.ts appearing in this zone is a misclassification signal; e2e tests should be anchored to the zone under test (likely Project Configuration or CLI and MCP Interface), not grouped with schema/store unit tests.
- Coupling of 0.83 confirms all three source files are highly dependent on external zones; once dissolved, the overall cross-zone import graph will be cleaner and these dependencies will be properly attributed to their owning zones.
- Zone "schema-storage-reporting" has files across 7 directories — consider consolidating under a dedicated directory
- schema-storage-reporting is the only zone in this batch where all three source files (report.ts, validate.ts, file-adapter.ts) are canonical members of three different pre-existing zones (rex-cli-command-layer, prd-structural-validation, integration-adapter-layer) — it is a pure detection artifact with zero legitimate cross-file cohesion
- The presence of e2e/cli-init.test.ts in this zone alongside unit tests for schema and store reveals that the community detection pulled in an e2e test based on shared imports with validate.ts and file-adapter.ts — this is a test-import contamination that should be corrected by pinning e2e tests to the e2e zone regardless of import overlap
- schema-storage-reporting is the only zone where dissolution eliminates zero genuine cross-file relationships — all three source files have independent homes in existing zones and share no domain purpose; dissolving this zone would reduce overall zone count by one without losing any architectural signal
- e2e/cli-init.test.ts was pulled into this zone because it imports both validate.ts and file-adapter.ts, demonstrating how e2e tests that import from multiple domain zones can contaminate zone assignments — zone detection should apply a blanket pin rule: tests/e2e/** always anchors to the e2e zone regardless of import neighbors
- The zone name is a concatenation of all three resident source files' responsibilities (schema=validate.ts, storage=file-adapter.ts, reporting=report.ts) — encoding zone confusion into the name itself is a naming anti-pattern; a well-named zone uses a single domain concept
- schema-storage-reporting and task-selection-engine share identical cohesion (0.17) and coupling (0.83) metrics — the probability of two zones landing at exactly the same values by coincidence is low; they are likely products of the same detection artifact (test file co-location pulling disparate source files into proximity) and would both be resolved by a single algorithm change
- file-adapter.ts (store implementation) and contracts.ts (store interface, in prd-domain-operations) are in different zones — the interface and its concrete implementation are zone-separated, making the ports-and-adapters boundary invisible to zone health tooling
- Dissolve this zone and pin each file to its natural home: report.ts → rex-cli-command-layer, validate.ts → prd-structural-validation, file-adapter.ts → integration-adapter-layer, e2e/cli-init.test.ts → e2e zone; the zone name itself signals it has no unifying domain concept
- [call graph] 146 internal calls, 16 outgoing, 22 incoming (cohesion: 0.9, coupling: 0.1)

</insights>
