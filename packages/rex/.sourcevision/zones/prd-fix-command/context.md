# PRD Fix Command — Zone Context

<!-- Per-zone context generated by Sourcevision. -->

<zone>

Zone: PRD Fix Command (`prd-fix-command`)
Files: 4, Cohesion: 0.25, Coupling: 0.75
Risk: catastrophic (score: 0.75)
Description: The isolated fix operation: a CLI command, its core implementation, and co-located unit tests.
Entry points: src/cli/commands/fix.ts
Lines: 1230

</zone>

<files>

src/cli/commands/fix.ts (TypeScript, 139 lines, source)
src/core/fix.ts (TypeScript, 258 lines, source)
tests/unit/cli/commands/fix.test.ts (TypeScript, 420 lines, test)
tests/unit/core/fix.test.ts (TypeScript, 413 lines, test)

</files>

<imports>

Internal:
  src/cli/commands/fix.ts → src/core/fix.ts {detectIssues, applyFixes}
  src/cli/commands/fix.ts → src/core/fix.ts {FixAction, FixKind}
  tests/unit/cli/commands/fix.test.ts → src/cli/commands/fix.ts {cmdFix}
  tests/unit/core/fix.test.ts → src/core/fix.ts {detectTimestampIssues, detectOrphanBlockedBy, detectParentChildMisalignment, detectIssues, applyFixes}

Outgoing (this zone → other zones):
  → mutation-commands: src/cli/commands/fix.ts → src/cli/output.ts
  → prd-operations-core: src/core/fix.ts → src/schema/index.ts; tests/unit/cli/commands/fix.test.ts → src/schema/index.ts; tests/unit/core/fix.test.ts → src/schema/index.ts
  → remote-integration: src/cli/commands/fix.ts → src/store/index.ts; src/core/fix.ts → src/core/tree.ts
  → rex-status-mcp-cli: src/cli/commands/fix.ts → src/cli/commands/constants.ts

Incoming (other zones → this zone):
  ← mutation-commands: src/cli/index.ts → src/cli/commands/fix.ts

</imports>

<findings>

[observation] [warning] High coupling (0.75) — 3 imports target "prd-operations-core"
[observation] [warning] Low cohesion (0.25) — files are loosely related, consider splitting this zone
[observation] [warning] Coupling of 0.75 in a 4-file zone means nearly all edges leave the zone; if fix logic can be expressed as a thin wrapper over existing core primitives, this is architecturally appropriate and not a problem.
[observation] [info] Having both unit tests (tests/unit/core/fix.test.ts, tests/unit/cli/commands/fix.test.ts) co-located in the same algorithmic zone as their source is a positive signal — the community detection reflects good structural locality.
[observation] [info] If fix.ts is the only consumer of core/fix.ts, the core module may be over-abstracted; verify whether fix logic could live directly in the CLI command without a separate core file.
[relationship] [warning] prd-fix-command and mutation-commands are both leaf command zones that should have no peer dependency on each other; the 1+1 bidirectional coupling is most likely a shared-constants artifact — confirm and remove to achieve clean fan-in-only topology for both zones
[suggestion] [info] Duplicate filename 'fix.ts' at src/core/fix.ts and src/cli/commands/fix.ts creates stack-trace and search ambiguity — rename the core module to src/core/fix-engine.ts or src/core/prd-fix.ts to distinguish the domain logic from the CLI entry point at a glance
[suggestion] [warning] prd-fix-command has BOTH the lowest cohesion (0.25) AND highest coupling (0.75) relative to its size (4 files) — this worst-ratio-per-file combination makes it the most fragile small zone in the package; if core/fix.ts remains a single-consumer module, collapsing core logic into the CLI command eliminates the zone entirely and removes one layer of indirection
[suggestion] [critical] Zone "PRD Fix Command" (prd-fix-command) has catastrophic risk (score: 0.75, cohesion: 0.25, coupling: 0.75) — requires immediate architectural intervention

</findings>

<insights>

- Low cohesion (0.25) — files are loosely related, consider splitting this zone
- High coupling (0.75) — 3 imports target "prd-operations-core"
- At only 4 files this is a well-scoped, self-contained feature — the tests travel with the implementation which is a good colocation pattern
- Cohesion 0.25 and coupling 0.75 are both concerning for such a small zone; the numbers suggest fix.ts imports heavily from core rather than being self-sufficient
- The zone's small size means the low cohesion metric is noisy — a single additional peer import dramatically shifts the score; treat the coupling figure as more meaningful here
- Coupling of 0.75 in a 4-file zone means nearly all edges leave the zone; if fix logic can be expressed as a thin wrapper over existing core primitives, this is architecturally appropriate and not a problem.
- Having both unit tests (tests/unit/core/fix.test.ts, tests/unit/cli/commands/fix.test.ts) co-located in the same algorithmic zone as their source is a positive signal — the community detection reflects good structural locality.
- If fix.ts is the only consumer of core/fix.ts, the core module may be over-abstracted; verify whether fix logic could live directly in the CLI command without a separate core file.
- The 1+1 bidirectional coupling with mutation-commands likely flows through constants.ts rather than direct logic sharing — if confirmed, removing the constants dependency eliminates the only peer coupling between these two leaf-command zones
- prd-fix-command and mutation-commands are both leaf command zones that should have no peer dependency on each other; the 1+1 bidirectional coupling is most likely a shared-constants artifact — confirm and remove to achieve clean fan-in-only topology for both zones
- src/core/fix.ts and src/cli/commands/fix.ts share the filename 'fix' across two directory levels — in stack traces and grep results both appear as 'fix.ts' with no distinguishing prefix, making debugging harder; a more specific name for the core module (e.g. prd-fix.ts or fix-engine.ts) would eliminate ambiguity without changing the public API
- Duplicate filename 'fix.ts' at src/core/fix.ts and src/cli/commands/fix.ts creates stack-trace and search ambiguity — rename the core module to src/core/fix-engine.ts or src/core/prd-fix.ts to distinguish the domain logic from the CLI entry point at a glance
- prd-fix-command has BOTH the lowest cohesion (0.25) AND highest coupling (0.75) relative to its size (4 files) — this worst-ratio-per-file combination makes it the most fragile small zone in the package; if core/fix.ts remains a single-consumer module, collapsing core logic into the CLI command eliminates the zone entirely and removes one layer of indirection
- [call graph] 136 internal calls, 15 outgoing, 1 incoming (cohesion: 0.9, coupling: 0.1)

</insights>
