# PRD Repair Command — Zone Context

<!-- Per-zone context generated by Sourcevision. -->

<zone>

Zone: PRD Repair Command (`prd-repair-command`)
Files: 4, Cohesion: 0.25, Coupling: 0.75
Risk: catastrophic (score: 0.75)
Description: Self-contained implementation of the `rex fix` command that detects and repairs structural inconsistencies in the PRD tree.
Entry points: src/cli/commands/fix.ts
Lines: 1230

</zone>

<files>

src/cli/commands/fix.ts (TypeScript, 139 lines, source)
src/core/fix.ts (TypeScript, 258 lines, source)
tests/unit/cli/commands/fix.test.ts (TypeScript, 420 lines, test)
tests/unit/core/fix.test.ts (TypeScript, 413 lines, test)

</files>

<imports>

Internal:
  src/cli/commands/fix.ts → src/core/fix.ts {detectIssues, applyFixes}
  src/cli/commands/fix.ts → src/core/fix.ts {FixAction, FixKind}
  tests/unit/cli/commands/fix.test.ts → src/cli/commands/fix.ts {cmdFix}
  tests/unit/core/fix.test.ts → src/core/fix.ts {detectTimestampIssues, detectOrphanBlockedBy, detectParentChildMisalignment, detectIssues, applyFixes}

Outgoing (this zone → other zones):
  → cli-mcp-interface: src/cli/commands/fix.ts → src/cli/commands/constants.ts
  → integration-adapter-layer: src/cli/commands/fix.ts → src/store/index.ts
  → prd-domain-operations: src/core/fix.ts → src/schema/index.ts; tests/unit/cli/commands/fix.test.ts → src/schema/index.ts; tests/unit/core/fix.test.ts → src/schema/index.ts
  → rex-cli-command-layer: src/cli/commands/fix.ts → src/cli/output.ts
  → tree-dag-primitives: src/core/fix.ts → src/core/tree.ts

Incoming (other zones → this zone):
  ← rex-cli-command-layer: src/cli/index.ts → src/cli/commands/fix.ts

</imports>

<findings>

[observation] [warning] High coupling (0.75) — 3 imports target "prd-domain-operations"
[observation] [warning] Low cohesion (0.25) — files are loosely related, consider splitting this zone
[observation] [info] Both unit tests (fix.test.ts) live inside this zone alongside their source files — good locality that simplifies future relocation if the zone is merged.
[observation] [warning] Cohesion of 0.25 is well below the 0.4 warning threshold for a zone this small — the low score likely reflects that fix.ts imports broadly from core utilities without a tight internal cluster, making this a candidate for dissolution into the prd-domain-operations zone.
[observation] [info] The zone has exactly one entry point (cli/commands/fix.ts), which is clean; if the zone is eventually dissolved, re-export fix.ts through the existing CLI command registry rather than adding a new entry point.
[pattern] [info] prd-repair-command is structurally identical to task-selection-engine: one CLI command file, one core implementation file, and N test files grouped by proximity — this recurring pattern suggests the zone detection algorithm consistently creates micro-zones around test clusters, not domain clusters
[relationship] [info] The mutual 1-import dependency between prd-repair-command and rex-cli-command-layer is likely a shared CLI output utility rather than a domain interface — verify that fix.ts is not importing formatting functions from sibling command files, which would indicate missing shared utility extraction
[suggestion] [info] Rename zone from 'prd-repair-command' to 'prd-fix-command' to match the canonical file names fix.ts and fix.test.ts; zone names should derive from the files they contain, not introduce synonyms
[suggestion] [info] Zone "PRD Repair Command" (prd-repair-command) has catastrophic risk (score: 0.75, cohesion: 0.25, coupling: 0.75) — unreliable: zone has only 4 files (minimum 5 for reliable metrics)

</findings>

<insights>

- Low cohesion (0.25) — files are loosely related, consider splitting this zone
- High coupling (0.75) — 3 imports target "prd-domain-operations"
- With only 4 files (2 source, 2 test) this is a micro-zone; it is well-tested relative to its size but should be monitored to ensure the fix logic does not grow without the zone boundary being reconsidered
- High coupling of 0.75 means fix.ts depends on many external modules for a small amount of domain-specific work — consider whether this logic belongs inside the core zone as a peer of cascade-reset or transitions
- The co-location of the CLI command and core implementation in one zone is consistent with how unit-core handles next-task; this pattern is acceptable for small, focused features
- Cohesion of 0.25 is well below the 0.4 warning threshold for a zone this small — the low score likely reflects that fix.ts imports broadly from core utilities without a tight internal cluster, making this a candidate for dissolution into the prd-domain-operations zone.
- The zone has exactly one entry point (cli/commands/fix.ts), which is clean; if the zone is eventually dissolved, re-export fix.ts through the existing CLI command registry rather than adding a new entry point.
- Both unit tests (fix.test.ts) live inside this zone alongside their source files — good locality that simplifies future relocation if the zone is merged.
- prd-repair-command, task-selection-engine, and schema-storage-reporting share an identical structural anti-pattern: community detection grouped test files with nearby source files rather than finding genuine domain cohesion — all three zones are test-adjacency artifacts, not architectural units
- The 1-import bidirectional coupling between prd-repair-command and rex-cli-command-layer is the smallest cycle in the package; at this scale it is likely a shared formatting helper rather than a real domain interface — audit whether fix.ts reuses output utilities from the CLI layer that belong in a shared utility module
- prd-repair-command is structurally identical to task-selection-engine: one CLI command file, one core implementation file, and N test files grouped by proximity — this recurring pattern suggests the zone detection algorithm consistently creates micro-zones around test clusters, not domain clusters
- The mutual 1-import dependency between prd-repair-command and rex-cli-command-layer is likely a shared CLI output utility rather than a domain interface — verify that fix.ts is not importing formatting functions from sibling command files, which would indicate missing shared utility extraction
- Zone name uses 'repair' but both source files use 'fix' as the canonical identifier (fix.ts, fix.test.ts) — naming mismatch increases cognitive overhead when searching for the fix command implementation
- fix.ts is the only major CLI command absent from rex-cli-command-layer, meaning a developer auditing CLI commands from that zone gets an incomplete picture of the command surface
- Rename zone from 'prd-repair-command' to 'prd-fix-command' to match the canonical file names fix.ts and fix.test.ts; zone names should derive from the files they contain, not introduce synonyms
- [call graph] 136 internal calls, 15 outgoing, 1 incoming (cohesion: 0.9, coupling: 0.1)

</insights>
