---
id: "544d93d2-10f8-45a6-815b-5a7664d6a65c"
level: "task"
title: "--review leaves the PRD completion metadata uncommitted when the agent commits its own work anyway"
status: "completed"
priority: "medium"
tags:
  - "ndx-adversarial-review"
  - "severity:medium"
  - "review-pass"
source: "ndx-adversarial-review"
startedAt: "2026-08-28T18:49:41.854Z"
completedAt: "2026-08-28T18:49:41.854Z"
endedAt: "2026-08-28T18:49:41.854Z"
resolutionType: "code-change"
resolutionDetail: "performCommitPromptIfNeeded now returns a CommitPromptOutcome and finalizeRun gates commitCompletionMetadata on it rather than on opts.autoCommit, so every route that produces no commit (including --review with a self-committing agent) cleans up the PRD metadata; declined and failed commits are deliberately excluded. The post-auto-commit leftover warning counts the whole working tree via git status --porcelain instead of the index alone. Tests added for both, verified red first."
acceptanceCriteria:
  - "After a --review run in which the agent committed its own work and left nothing staged and no .hench-commit-msg.txt, finalizeRun commits the PRD completion metadata instead of leaving .rex/prd_tree dirty"
  - "A test covers: review-enabled run, agent self-commit, no message file → git status is clean after finalizeRun"
  - "The post-auto-commit leftover warning (shared.ts:1507) counts unstaged and untracked leftovers, not only the index, so it cannot print the quiet 'proceeding to next task' acknowledgment while .rex/prd_tree or a review-pass repair is still dirty"
  - "A test leaves a review repair unstaged, stubs didAutoCommit() to true, and asserts the leftover warning still fires (the existing test at review-watcher-suspension.test.ts:278 stages with git add -A first, so the unstaged case is untested)"
description: "Verdict: should-fix (severity low). --review forces effectiveAutoCommit off (cli-loop.ts:1500-1509), so commitCompletionMetadata (shared.ts:2122) — gated on opts.autoCommit — never runs on a review-enabled run. The executor is only instructed not to commit; git commit stays in its allowed commands, and the HEAD-move warning at the top of runAdversarialReviewPass exists because that path is anticipated. Scenario: --review run with hench.autoCommit true; the agent follows its autoCommit habit, commits its work itself, and leaves nothing staged and no .hench-commit-msg.txt → performCommitPromptIfNeeded exits early (no message file, or stagedCount 0 at shared.ts:1533), and the PRD completion metadata written by updateCompletedTaskStatus (shared.ts:2081) is left dirty in .rex/prd_tree. The next run's pre-run commit gate sweeps it into a 'chore: commit local changes before hench run' commit attributed to nothing.\n\nReachability: any --review run where the spawned agent self-commits — the same disobedience path the HEAD-move warning already reports.\n\nSolutions: (A, recommended) commit the completion metadata whenever the run completed but this finalize made no commit — i.e. relax the opts.autoCommit gate on commitCompletionMetadata to also cover the review-override case where performCommitPromptIfNeeded returned without committing; a few lines plus a test. (B) extend the HEAD-move warning to say the PRD completion metadata will be left uncommitted — honesty only, the residue remains.\n\n---\n\n## Third early-return route, and a warning that misreports it\n\nAdded by ndx-adversarial-review on 2026-08-28, reviewing commit 0c23335f (\"fix(hench): suspend the commit-msg watcher while the review pass runs\"). Severity raised low → medium: there is a third route to this same residue, and on that route hench actively reports the situation as benign.\n\nVerdict: should-fix (severity medium).\n\nRoute: the commit-msg watcher's auto-commit timer fires — pre-review, or before the suspension that 0c23335f added — so didAutoCommit() is true and performCommitPromptIfNeeded returns at shared.ts:1517. That is a third early return alongside the two this item already names (1520 !existsSync, 1534 stagedCount === 0). Same residue: updateCompletedTaskStatus (shared.ts:2081) has already written .rex/prd_tree with no git add, so it is left dirty.\n\nWhat is new and worse: 0c23335f added a leftover warning at shared.ts:1507-1513 for exactly this branch, but it counts only the index — countStagedFiles is `git diff --cached --name-only` (shared.ts:1085-1095). The PRD metadata write is unstaged, and review-pass repairs are unstaged too (the reviewer's brief forbids committing and never instructs `git add` — adversarial-review.ts:322-325). So stagedAfterAuto is 0, the else branch runs, and the operator is told \"Auto-commit: timer-expiry auto-commit acknowledged — proceeding to next task.\" while real work sits dirty. Left silent, those files ride the next run's `git add -A` and get attributed to unrelated work — the exact harm the new comment names.\n\nRefutation attempted: searched for any git add between the auto-commit and this check. The only staging in performCommitPromptIfNeeded is the .rex/ block at shared.ts:1601-1620, which is AFTER this early return, and runAdversarialReviewPass stages nothing.\n\nReachability: any --review run whose auto-commit timer fires before the suspension — e.g. the executor writes .hench-commit-msg.txt and then keeps working past commitMsgTimeoutMs (default 300_000).\n\nNot covered: the new test stages with `git add -A` before asserting (review-watcher-suspension.test.ts:239-240), so only the staged case is exercised.\n\nSolution A above already fixes the residue on this route, since didAutoCommit() is precisely \"performCommitPromptIfNeeded returned without committing\". It does NOT fix the misreport: the leftover count must also look beyond the index (`git status --porcelain`), or the warning will keep calling a dirty tree clean. Do both.\n\nRelated: e168b824-aa84-4e7b-b43a-ad2833b113d6 covers the unstaged-reviewer-repairs half of the same residue; its solution B (`git add -u` after a review pass reporting fixesApplied) would stage repairs but still not the PRD write, which happens after the review pass."
lastModified: "2026-08-28T18:49:41.882Z"
lastModifiedBy: "Sterling H <sterling.h@endash.us>"
---
