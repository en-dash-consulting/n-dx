---
id: "773385ae-1b44-4572-a769-53a959fcc306"
level: "task"
title: "Make the dashboard next-task read skip live claims so it matches Execute"
status: "completed"
priority: "medium"
tags:
  - "0.7.1"
  - "claims-hardening"
  - "wm-2047"
source: "caos work management: WM2047 (Make the dashboard next-task read skip live claims so it matches Execute); 0.7.1 execution plan PR group"
startedAt: "2026-09-24T14:53:08.890Z"
completedAt: "2026-09-24T14:59:29.756Z"
endedAt: "2026-09-24T14:59:29.756Z"
resolutionType: "code-change"
resolutionDetail: "next and dashboard reads exclude the request workspace's foreign live claims via findNextTask's excludeIds (the set Execute's 409 consults), report skipped/nextTaskSkipped with task id, title and claiming worktree, and the Up Next card renders the note. Claims endpoint and chips unchanged; workspace-scoped resolution respected. Five unit tests including the next/dashboard-vs-Execute agreement. Commit f7b21554 on feat/claims-hardening-pr-e."
acceptanceCriteria:
  - "Unit test: with a live foreign claim on the highest-priority task, the next-task route returns the next unclaimed task and the Execute route accepts it."
  - "The response includes, when a higher-priority task was skipped because of a claim, that task's id and the claiming worktree, so the card can say so."
  - "GET /api/rex/claims and the PRD tree chip are unchanged."
  - "The workspace-scoped store resolution introduced in 0.7.0 is respected (the read uses the request's workspace)."
description: "The dashboard's next-task read (GET route in packages/web/src/server/routes-rex/reads.ts, using findNextTask from rex through the web rex-gateway) does not consult the claims store, while the Execute route in routes-hench.ts refuses a claimed task with 409. The Home and Rex landing cards can therefore suggest a task that Execute then rejects. Pass the live claims (openClaimsStore(ctx.projectDir).readClaims(), already used by GET /api/rex/claims in the same file) into the selection the same way hench does, so the suggestion is always a task Execute will accept, and show the holder for a task that was skipped for that reason.\n\nImplementation notes: In packages/web/src/server/routes-rex/reads.ts, make the next-task handler read live claims from the claims store for the request's workspace and pass them to rex's findNextTask exclusion in the same way hench's selection does (see how hench passes claims through packages/hench/src/prd/rex-gateway.ts to rex's core/next-task.ts). Add to the response an optional 'skipped' entry naming any higher-priority task that was passed over because another worktree holds it, with the worktree path. Update the Home / Rex landing next-task card in the viewer to show 'claimed by <worktree>' when present. Add a unit test in packages/web/tests/unit/server covering the agreement between next-task and Execute with a fixture claim. All rex imports through packages/web/src/server/rex-gateway.ts. Constraints that apply to every n-dx change: cross-package imports go only through the package's gateway module (hench: src/prd/rex-gateway.ts and src/prd/llm-gateway.ts; web: src/server/rex-gateway.ts and src/server/domain-gateway.ts) and tests/e2e/architecture-policy.test.js enforces an export ceiling on those gateways; orchestration scripts in packages/core spawn CLIs and never import packages; every user-facing change carries a changeset using the scoped package name (@n-dx/hench, @n-dx/rex, @n-dx/web, @n-dx/core, @n-dx/sourcevision, @n-dx/llm-client) with a patch bump; run pnpm preflight before opening the PR."
lastModified: "2026-09-24T14:59:31.737Z"
lastModifiedBy: "Sterling H <sterling.h@endash.us>"
---
