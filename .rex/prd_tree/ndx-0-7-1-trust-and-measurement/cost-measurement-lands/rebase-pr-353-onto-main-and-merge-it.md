---
id: "2b86bc48-6cab-4cd1-bc15-b05ee38fc14d"
level: "task"
title: "Rebase PR #353 onto main and merge it"
status: "completed"
priority: "high"
tags:
  - "0.7.1"
  - "cost-measurement"
  - "wm-2050"
source: "caos work management: WM2050 (Rebase PR #353 onto main and merge it); 0.7.1 execution plan PR group"
startedAt: "2026-09-22T21:06:47.261Z"
completedAt: "2026-09-22T21:06:47.261Z"
endedAt: "2026-09-22T21:06:47.261Z"
resolutionType: "code-change"
resolutionDetail: "Work shipped as PR #390 (rebase of #353 merged to main at 11634bb4) — all 15 changesets landed; no separate rebase needed."
acceptanceCriteria:
  - "The branch is rebased onto main with all 15 changesets intact and no unrelated file changes."
  - "Conflicts in packages/hench/src/agent/lifecycle/shared.ts, session-cache.ts, orientation.ts and loop.ts are resolved so both the 0.7.0 fixes and the caching behaviour remain, each covered by its existing tests."
  - "`pnpm preflight` is green; the full suite passes under concurrent build load."
  - "The PR is merged to main."
description: "PR #353 (branch feature/auditing-improvements-batch-2) carries prompt caching on the Anthropic API loop, a summarizing prune that replaces dropped turns with a light-tier digest, primer-seeded orientation, cache-token reporting in the run summary, per-model pricing, compaction cost recording, a token budget that counts cached input, and a test-gate diagnostics fix, with 15 patch changesets. It is 23 commits ahead of and 11 behind main and has never run against a live provider; every run that built it executed a separate checkout. Rebase it across the 0.7.0 merges (#369, #370, #376), which touched the same lifecycle files, resolve conflicts, and merge.\n\nImplementation notes: Rebase branch feature/auditing-improvements-batch-2 (PR #353) onto current main. Expect conflicts in packages/hench/src/agent/lifecycle/shared.ts, session-cache.ts, orientation.ts, loop.ts and packages/hench/src/schema/v1.ts, where #369, #370 and #376 changed run-record fields, claims, and the completion gate; keep both sides' behaviour and rerun the unit tests that pin each (context-prune, prompt-cache, orientation, primer, token-budget, shared-lifecycle in packages/hench/tests/unit/agent, plus task-claims-selection and completion-metadata-commit in tests/integration). Verify TESTING.md, ENFORCEMENT.md and PACKAGE_GUIDELINES.md edits from the branch still apply. Run `pnpm preflight`, then mark the PR ready and merge. Do not squash away the changesets. Constraints that apply to every n-dx change: cross-package imports go only through the package's gateway module (hench: src/prd/rex-gateway.ts and src/prd/llm-gateway.ts; web: src/server/rex-gateway.ts and src/server/domain-gateway.ts) and tests/e2e/architecture-policy.test.js enforces an export ceiling on those gateways; orchestration scripts in packages/core spawn CLIs and never import packages; every user-facing change carries a changeset using the scoped package name (@n-dx/hench, @n-dx/rex, @n-dx/web, @n-dx/core, @n-dx/sourcevision, @n-dx/llm-client) with a patch bump; run pnpm preflight before opening the PR."
lastModified: "2026-09-22T21:06:48.015Z"
lastModifiedBy: "Sterling H <sterling.h@endash.us>"
---
