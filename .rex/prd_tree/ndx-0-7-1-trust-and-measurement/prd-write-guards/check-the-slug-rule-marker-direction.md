---
id: "078cd2b1-3b60-4f22-a666-6ffa6bf22345"
level: "task"
title: "Check the slug-rule marker direction inside the PRD lock in adoptSlugRule (both stores)"
status: "completed"
priority: "high"
tags:
  - "0.7.1"
  - "prd-write-guards"
  - "wm-2094"
  - "pr-j2"
source: "caos work management: WM2094 (Check the slug-rule marker direction inside the PRD lock in adoptSlugRule); follow-up from the guards run 2026-09-22, PR group J2"
startedAt: "2026-09-22T20:14:48.077Z"
completedAt: "2026-09-22T21:01:00.979Z"
endedAt: "2026-09-22T21:01:00.979Z"
resolutionType: "code-change"
resolutionDetail: "Moved the marker read/direction check from adoptSlugRule (pre-lock) into writeTree, which runs inside runTransaction's locked callback, immediately ahead of mkdir(treeRoot). Added a regression test using the real file-lock to hold the PRD lock, write a newer marker while adoptSlugRule waits, then release — confirmed it fails without the fix and passes with it."
acceptanceCriteria:
  - "FileStore.adoptSlugRule performs no marker read before the lock; the direction check runs inside writeFolderTree, under the PRD lock, before mkdir creates any directory."
  - "The concurrent-marker regression test runs against both adapters through the existing describe.each in slug-rule-guard.test.ts, and is confirmed by hand to fail against the pre-fix FileStore."
  - "The FolderTreeStore behaviour landed in e145d7be is unchanged, and its test still passes."
  - "grep for adoptSlugRule across packages/rex/src shows no remaining pre-lock marker read in either adapter."
  - "The changeset at .changeset/slug-guard-adopt-under-lock.md is amended to name both adapters and to say that rex migrate-slugs, which resolves to FileStore, is the path the fix protects."
  - "Full root pnpm test passes, 6/6 suites."
description: "REOPENED 2026-09-22. The first pass (commit e145d7be) fixed FolderTreeStore only, and FolderTreeStore never runs in production — so the race this task exists to close is still live on the only path that can reach it. The original description named only folder-tree-store.ts, which is the error being corrected here.\n\nWHERE IT STANDS. Commit e145d7be moved the marker read out of FolderTreeStore.adoptSlugRule and into FolderTreeStore.writeTree, which runs inside runTransaction's locked callback, immediately ahead of mkdir(treeRoot), branching on the adopt flag: assertSlugRuleAdoptable for the adopt-older direction, assertSlugRuleWritable for the exact-match case. It added a regression test at packages/rex/tests/unit/store/slug-rule-guard.test.ts that holds the real PRD lock as a stand-in concurrent writer. That work is correct and stays. The shape it established is the shape to copy.\n\nWHAT REMAINS. FileStore has the identical bug and is the store that actually runs. resolveStore at packages/rex/src/store/index.ts:262 returns new FileStore, and FolderTreeStore has zero production construction sites — grep for `new FolderTreeStore` outside packages/rex/tests returns nothing. rex migrate-slugs resolves its store through resolveStore at packages/rex/src/cli/commands/migrate-slugs.ts:53 and is the only production caller of adoptSlugRule, so every real migration takes the unfixed path. FileStore.adoptSlugRule at packages/rex/src/store/file-adapter.ts:558 still calls assertSlugRuleAdoptable before runTransaction takes the lock, and then passes adoptSlugRule=true into writeFolderTree at line 494, which skips the guard outright — a concurrent writer that records a newer marker between the read and the lock has it silently overwritten by the migration.\n\nPRIOR ART, WHICH THIS TASK REPEATED. The #384 run recorded the same trap in .rex/execution-log.jsonl under item 5c09a992: \"DESIGN POINT THE TASK DESCRIPTION UNDERSTATED: the guard could not live only in folder-tree-store.ts. resolveStore() returns a FileStore, not a FolderTreeStore, so every CLI and MCP write goes through file-adapter.ts — a guard on FolderTreeStore alone would have left the entire ordinary write path unprotected.\" Treat any slug-guard change as landing in both adapters unless the call graph says otherwise; do not take a file path in a task description as the boundary of the fix.\n\nImplementation notes: mirror e145d7be into packages/rex/src/store/file-adapter.ts. Delete the pre-lock assertSlugRuleAdoptable call in adoptSlugRule (line 558) so the method is just the runTransaction call, and in writeFolderTree (line 494) replace the `if (!adoptSlugRule) { await assertSlugRuleWritable(...) }` with a branch that calls assertSlugRuleAdoptable(this.rexDir) when adoptSlugRule is true and assertSlugRuleWritable(this.rexDir, this.treeRoot) otherwise, both read fresh under the lock and ahead of the mkdir on line 502. Note that FileStore also has withFileTransaction, which #384 collapsed onto writeFolderTree precisely so a guard could not cover one path and miss the other — confirm that is still true so the branch is reached from every write.\n\nFor the test: packages/rex/tests/unit/store/slug-rule-guard.test.ts already runs a describe.each over both adapters at line 76. The block e145d7be added at line 330 opted out of it with a FolderTreeStore-only comment; fold it into the parametrized harness instead, so the same concurrent-marker scenario runs against both stores. Confirm by hand that it fails against the pre-fix FileStore before the change and passes after — a test that only ever saw FolderTreeStore is what let the gap through the first time.\n\nAmend the existing changeset at .changeset/slug-guard-adopt-under-lock.md rather than adding a second one; it has not shipped, it names only FolderTreeStore, and as written it would become release copy claiming rex migrate-slugs is fixed when the command's own store was untouched. Run the full root pnpm test, not only the rex package suite — the first pass scoped its validation to rex and a monorepo typecheck, which cannot see a store-selection problem.\n\nConstraints that apply to every n-dx change: cross-package imports go only through the package's gateway module (hench: src/prd/rex-gateway.ts and src/prd/llm-gateway.ts; web: src/server/rex-gateway.ts and src/server/domain-gateway.ts) and tests/e2e/architecture-policy.test.js enforces an export ceiling on those gateways; orchestration scripts in packages/core spawn CLIs and never import packages; every user-facing change carries a changeset using the scoped package name with a patch bump; run pnpm preflight before opening the PR."
lastModified: "2026-09-22T21:01:01.313Z"
lastModifiedBy: "Ryan Keith <ryan.k@endash.us>"
---
