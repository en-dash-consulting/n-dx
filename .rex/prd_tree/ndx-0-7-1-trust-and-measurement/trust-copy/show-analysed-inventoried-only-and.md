---
id: "41687109-c7f3-40e4-98c4-627a527dd5be"
level: "task"
title: "Show analysed, inventoried-only and skipped languages with counts on the Files page"
status: "completed"
priority: "medium"
tags:
  - "0.7.1"
  - "trust-copy"
  - "wm-2043"
source: "caos work management: WM2043 (Show analysed, inventoried-only and skipped languages with counts on the Files page); 0.7.1 execution plan PR group"
startedAt: "2026-09-23T19:51:24.551Z"
completedAt: "2026-09-23T20:08:31.972Z"
endedAt: "2026-09-23T20:08:31.972Z"
resolutionType: "code-change"
resolutionDetail: "Added skippedExtensions + analysedLanguages to sourcevision's inventory summary (optional, additive) and a LanguageAnalysisStrip above the Files table in web reading them, degrading to a neutral \"Inventoried\" group on old data."
acceptanceCriteria:
  - "sourcevision's inventory summary gains an optional field recording skipped file extensions with counts; inventories written before the change still load (test with an old fixture)."
  - "On a fixture project containing .zig files, the Files page strip reads, for example, 'Analysed: TypeScript (412), JavaScript (88) · Inventoried only: Markdown (31) · Skipped: .zig (14), .toml (3)'."
  - "The strip appears above the existing table with no new route or view id."
  - "Unit tests cover the analyzer field and the strip rendering."
description: "The dashboard showed only JavaScript files for a repository where Zig mattered, and nothing said Zig had been skipped. The Files view (packages/web/src/viewer/views/files.ts) already has inventory.summary.byLanguage for the languages that were inventoried. Add a strip above the table that states which languages were analysed (import graph and zones), which were inventoried only, and which extensions were skipped, each with file counts. The inventory analyzer in sourcevision must record skipped extensions and their counts as an additive summary field for this to be possible.\n\nImplementation notes: In packages/sourcevision, extend the inventory analyzer so its summary records, alongside byLanguage, an optional map of skipped file extensions to counts (files the walker saw but did not inventory because the extension is unsupported), and distinguish languages that are inventoried only from those that take part in import-graph and zone analysis. Keep the field optional so old inventory files load. In packages/web/src/viewer/views/files.ts add a strip above the table showing the three groups with counts, reading the new field when present and degrading to the two known groups when absent. Add a fixture with .zig files under the sourcevision tests and unit tests for both the analyzer and the view. Constraints that apply to every n-dx change: cross-package imports go only through the package's gateway module (hench: src/prd/rex-gateway.ts and src/prd/llm-gateway.ts; web: src/server/rex-gateway.ts and src/server/domain-gateway.ts) and tests/e2e/architecture-policy.test.js enforces an export ceiling on those gateways; orchestration scripts in packages/core spawn CLIs and never import packages; every user-facing change carries a changeset using the scoped package name (@n-dx/hench, @n-dx/rex, @n-dx/web, @n-dx/core, @n-dx/sourcevision, @n-dx/llm-client) with a patch bump; run pnpm preflight before opening the PR."
lastModified: "2026-09-23T20:08:32.337Z"
lastModifiedBy: "Ryan Keith <ryan.k@endash.us>"
---
