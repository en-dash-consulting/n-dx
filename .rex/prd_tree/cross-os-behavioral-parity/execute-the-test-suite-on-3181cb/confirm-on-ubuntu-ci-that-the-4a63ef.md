---
id: "4a63ef64-622d-40a5-b948-04dbd306d1f6"
level: "task"
title: "Confirm on ubuntu CI that the hench/web failures are Windows-specific as predicted"
status: "completed"
priority: "medium"
tags:
  - "cross-os"
  - "ci"
  - "testing"
  - "verification"
  - "hench"
  - "web"
source: "exploration-2026-08-18"
startedAt: "2026-08-19T19:02:12.630Z"
completedAt: "2026-08-19T19:05:23.911Z"
endedAt: "2026-08-19T19:05:23.911Z"
resolutionType: "acknowledgment"
resolutionDetail: "Prediction REFUTED, the outcome this task called more valuable. ubuntu's per-package step failed on the first run after the masking fix (32188420459) and went green on the next (32190469600); hench was not green as predicted — three test-runner cases asserted Windows behaviour unconditionally via hardcoded backslash paths — and llm-client failed too, a package the prediction never named. The web half proved untestable rather than confirmed: its two graph-view cases were fixed by a commit ordered earlier than the first run, so the window closed before it opened. Misattributed cause identified (group 1's \"passes on POSIX by construction\" argument holds for paths the code builds, not literals a test hands in) and corrected in feature 214f5636, along with a new third category: llm-client's POSIX tree kill never worked, a production defect with no Windows counterpart since taskkill needs no process group. Limit: per-package counts are unrecoverable — one aggregated step, and job logs are 403 without admin."
acceptanceCriteria:
  - "DONE, with one limit. First CI run after the masking fix: GitHub run 32188420459 (sha 6d1b35ee, 2026-08-18T21:34Z) — ubuntu \"Run unit tests\" step (= run-all-tests.mjs packages) FAILED, and \"Run root e2e\" was skipped as a result. Per-package pass/fail COUNTS are not recoverable: that step aggregates all five packages into one conclusion, and job-log download needs admin rights (403 unauthenticated). The failing PACKAGES are known from the commit that fixed them (a29966f9, pre-rebase 764e3ef1, whose body opens \"ubuntu CI caught two regressions I shipped\"): llm-client and hench. ubuntu went green on the very next run (32190469600) and has been green in all 14 runs since."
  - "DONE — THE PREDICTION IS REFUTED, on both halves and for different reasons. (a) hench was NOT green: three test-runner cases failed on ubuntu because they asserted Windows behaviour unconditionally. toCommandPath splits on `sep`, deliberately the identity on POSIX since a backslash is a legal POSIX filename character, and the tests fed HARDCODED BACKSLASH paths expecting normalization — so they could only ever pass on Windows. (b) The web half could not be tested at all: the two graph-view cases had already been fixed by e1ac1cfe, which is ORDERED EARLIER in the branch than the first run's commit, so by the time any ubuntu run existed the predicted failures no longer could occur. The window to falsify that half closed before it opened. (c) llm-client failed too, and the prediction named only hench and web."
  - "DONE. The misattributed cause is root-cause group 1 in feature 214f5636, \"POSIX path separators hardcoded in assertions\" (15 cases). Its stated basis was that the group \"passes on POSIX by construction because node's join() yields forward slashes there\". That argument is unsound rather than merely optimistic: it holds for paths the CODE BUILDS, not for paths a TEST HANDS IN as a literal. Three of the fifteen were the latter. The classification's broader claim — that all 32 are test-side with the production code correct — held for these three (the fix was test-side), but the reasoning used to reach it did not, and it is the reasoning that would have been reused on the remaining twelve."
  - "DONE. Feature 214f5636's description now carries a CORRECTION block recording: that \"ALL 32 are Windows-specific\" is wrong for at least three; that group 1's by-construction argument must not be reused; that \"zero production bugs\" did not survive the same run (see AC 5); and the transferable lesson that a Windows-only triage can only classify failures it can SEE. Group 1's line in the root-cause list is annotated inline so a reader scanning the table cannot miss it."
  - "DONE — one new category, and it is a PRODUCTION defect rather than a fixture issue. llm-client's POSIX tree kill had NEVER WORKED: execFile builds its own spawn options and silently drops `detached`, so the child was never a process-group leader, kill(-pid) failed with ESRCH, and only `sh` was killed. CI observed the grandchild write 13 more files AFTER the timeout was reported. It has no Windows counterpart by construction — taskkill /T walks the tree by PID and needs no group, so Windows passed regardless — and it was invisible to the Windows triage for exactly that reason. Its own commit body states the general form: \"Injected seams prove the shape of a decision, not that the syscall underneath does what you think.\" Recorded as a distinct category, not folded into the seven Windows-specific groups."
description: "Split out of task 741bacf1, whose other criteria are met. That task asked whether the hench/web failures reproduce on ubuntu; both suites have now been triaged in full ON WINDOWS, which turns the open question into a falsifiable prediction. Reading the first CI run either confirms it or exposes something the analysis missed.\n\nWHY THIS CANNOT BE ANSWERED LOCALLY: no macOS or Linux machine is available to this setup, `gh` is not on PATH, and the branch carrying the masking fix is unpushed — so no CI run exists yet. Unblocking needs a human to push and open a PR, which is an outward-facing action.\n\nTHE PREDICTION, derived from the triage rather than guessed:\n\n  @n-dx/hench — expect GREEN on ubuntu (all 32 Windows-specific).\n    Every one traces to a failure mode that cannot occur on POSIX: drive-qualified `C:\\` paths,\n    `\\r\n` from core.autocrlf, EBUSY on rmdir (POSIX permits unlinking open files), cmd.exe not\n    stripping single quotes, and backslash path separators where node's join() yields forward\n    slashes on POSIX by construction.\n\n  @n-dx/web — expect exactly 2 FAILURES on ubuntu, both in\n    tests/unit/viewer/graph-view.test.ts (\"recenters the file street view when the focused graph\n    changes\" and \"supports back and forward through clicked dependency preview nodes\").\n    Those are jsdom's getBoundingClientRect returning 0x0 with no stub anywhere in\n    packages/web/tests, which is platform-independent. The other five (boundary-check x2,\n    routes-sourcevision x1, routes-hench-execute x2) are Windows-specific by the same reasoning as\n    hench's.\n\n  Note routes-hench-execute's pair is INTERMITTENT on Windows (2 of 3 runs), so its absence from a\n  single ubuntu run is weak evidence either way.\n\nWHAT TO DO WITH THE RESULT:\n- Matches the prediction: record the confirmation here and close. The Windows-specific classification in\n  feature 214f5636 is then evidence-backed rather than analytical, which matters because those fix tasks\n  are scoped on the assumption that production code is correct and only fixtures need changing.\n- Does NOT match: that is the more valuable outcome and needs care. A hench failure on ubuntu means at\n  least one of the 32 has a cause the triage misattributed — find which, and correct the classification\n  in 214f5636 before its fix tasks are worked, since a task that says \"fixture bug, production is fine\"\n  would send someone down the wrong path.\n- A failure on ubuntu that does NOT appear on Windows would be a third category nobody has looked for.\n\nRecord the ubuntu counts per package alongside the Windows figures already captured in 214f5636, so the\ntwo platforms can be compared directly rather than through prose."
---
