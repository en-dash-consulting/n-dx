---
id: "8b16401f-3532-4cd9-9d8b-90e128c1fa24"
level: "task"
title: "Triage the first macOS CI run of the root suite"
status: "completed"
priority: "high"
tags:
  - "cross-os"
  - "ci"
  - "macos"
  - "testing"
  - "verification"
source: "exploration-2026-08-17"
startedAt: "2026-08-19T18:56:41.723Z"
completedAt: "2026-08-19T19:00:30.835Z"
endedAt: "2026-08-19T19:00:30.835Z"
resolutionType: "acknowledgment"
resolutionDetail: "Verification task, no code change. First macOS root-suite run was 32190469600 (green); macOS green in 14 of 15 branch runs. The single failure (32283838405) was my own over-strict assertion about POSIX process reaping, triaged individually and fixed in f58c2cb5 rather than skipped — and notably none of the five macOS causes this task predicted was involved. No macOS-specific skips exist. Observed billed cost is 63 added minutes per run with macOS at 63%, against an estimate of ~38 at ~80%: total understated, macOS share overstated, because Windows is slower than assumed and macOS's root step is actually faster. Decision: KEEP macOS, on evidence rather than cost — on run 32283838405 ubuntu passed the identical suite that macOS failed, so macOS caught a POSIX defect ubuntu missed, refuting this task's own \"ubuntu already covers POSIX\" premise."
acceptanceCriteria:
  - "DONE. First macOS run of the root suite: workflow run 32190469600 (sha 764e3ef1, 2026-08-18T21:58:31Z) — step \"Run root e2e / integration tests\" GREEN. The branch's very first run (32188420459) skipped macOS entirely because the smoke jobs gate on Build & Validate, which failed. Across all 15 runs on the branch macOS was green in every one except 32283838405. Suite size, from the run output: 96 files / 2113 tests / 1 skipped — identical to Windows and to a local Windows run, so macOS is not silently running a smaller suite. Per-test counts for the first run specifically are not recoverable: job-log download needs admin rights (403 unauthenticated), so step conclusions plus run output are the evidence."
  - "DONE, and there was exactly one macOS failure to triage: run 32283838405 (sha bc463c61), root e2e. Cause: tests/e2e/pair-programming-timeout-tree-kill.test.js asserted a killed child was already gone the instant the promise settled. That holds on Windows, where taskkill /T /F removes the tree synchronously, but not on POSIX — SIGKILL delivery and reaping are asynchronous and kill(pid, 0) still succeeds for a killed-but-unreaped process. Fixed in f58c2cb5 by bounding the wait (the frozen tick count carries the real claim), NOT by skipping on macOS or disabling the job. Triaged individually as required."
  - "DONE — there are NO macOS-specific skips, so none needs an OS-limitation citation. The four `darwin` references in tests/ are inputs to platform-parameterised assertions that run on any host, not skips. The root suite's single skip is tests/e2e/cli-init.test.js:673, skipped for a documented behavioural reason (per-vendor init failure isolation), and tests/unit/git-preflight.test.js gates on git being present — a capability check, not an OS one."
  - "DONE, and the estimate was wrong in both directions. Observed on run 32286589689 (green), job wall-clock and billed minutes at GitHub private-repo multipliers (Linux 1x, Windows 2x, macOS 10x, each job rounded up to the minute): Build & Validate ubuntu 7.12min → 8; CLI Smoke macOS 3.52min → 40; CLI Smoke Windows 10.70min → 22; CLI Smoke Parity 0.17min → 1. Total 71 billed min/run, of which the matrix expansion adds 63 (macOS 40 + Windows 22 + parity 1). The estimate said ~38 added minutes with macOS about 80% of them; actual is 63 added with macOS at 63%. Total cost was UNDERSTATED and macOS's share OVERSTATED, because Windows is slower than assumed (10.7min wall, its per-package step alone 6.13min). Root-suite step wall-clock: macOS 2.53min vs Windows 3.28min — macOS is the faster of the two."
  - "DONE — KEEP macOS, and the reasoning is now evidence rather than the cost argument this task was leaning on. On run 32283838405 ubuntu's root e2e step PASSED while macOS's FAILED, same suite and same commit. macOS caught a real POSIX-semantics defect that ubuntu did not, which directly refutes this task's premise that macOS carries 'the least incremental information' because ubuntu already covers POSIX. On that same run Windows independently caught a different real defect (exec resolving a timeout before the tree was dead, surfacing as EBUSY in per-package). Each non-ubuntu platform earned its place on a single run. Revisit only if CI cost becomes an actual constraint; if it does, the 40 billed minutes are worth one more look at whether the macOS job needs the full root suite or a narrower POSIX-semantics subset."
description: "The sibling CI-matrix task required a local full-suite run on Windows AND macOS before wiring CI, to size the damage in advance. Windows was measured directly (90 files, 2063 passed, 1 skipped, 0 failed, 80s wall on Windows 11). macOS COULD NOT BE MEASURED — there is no macOS machine available to this project's current development setup, and no amount of local work substitutes for it.\n\nThe matrix was wired for macOS anyway, on the reasoning that macOS is POSIX like the already-fully-green ubuntu job, so its risk profile is far closer to ubuntu's than to Windows's. That is an argument, not evidence. The first CI run on macos-latest IS the measurement, and it needs a human to look at it.\n\nWHAT TO DO on the first PR that runs the expanded matrix:\n1. Read the `CLI Smoke (macOS)` job's \"Run root e2e / integration tests\" step.\n2. If green: record the file/test/duration counts here and close. Compare wall-clock against the Windows job to validate or correct the billed-minute estimate below.\n3. If red: triage per failure rather than disabling the job. Likely macOS-specific causes, in rough order of probability:\n   - case-insensitive HFS+/APFS paths (a test asserting two paths differ, or relying on case-sensitive lookup)\n   - BSD vs GNU userland in any test that shells out (`sed -i`, `stat`, `date` flag differences)\n   - `/private/var` vs `/var` symlink for tmpdir — a test comparing a realpath'd temp path against the raw one will mismatch\n   - stricter default file-descriptor / process limits than ubuntu\n   - Gatekeeper/quarantine on freshly written executable fixtures\n4. Quarantine anything genuinely macOS-specific with an explicit reason, not a bare skip — the parent epic requires remaining skips to cite a real OS limitation.\n\nCOST NOTE TO VALIDATE: macOS is billed at 10x Linux minutes on GitHub-hosted private repos (Windows is 2x). Estimated from local timings, the macOS job accounts for roughly 30 of the ~38 added billed minutes per run — about 80% of the cost of this whole matrix expansion, for the least incremental information, since ubuntu already covers POSIX domain behaviour. If CI cost becomes a problem, dropping macOS and keeping Windows is the right trade, and this task should record that decision rather than leaving it implicit."
---
