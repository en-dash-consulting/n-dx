---
id: "1d05adea-488f-4120-8dc3-0138ac5f8240"
level: "task"
title: "Hench task display branches on rex prose, so a reworded selection summary silently blanks the finalize label"
status: "pending"
priority: "low"
acceptanceCriteria: []
description: "FAILURE SCENARIO. formatSelectionSummary in packages/hench/src/agent/lifecycle/task-display.ts:79-85 decides what to print by substring-matching prose that rex built:\n\n    if (explanation.summary.includes(\"already in_progress\")) ...\n    else if (explanation.summary.includes(\"all children completed\")) ...\n    else `${explanation.priority.itemPriority} priority`\n\nBoth strings are produced by explainSelection in packages/rex/src/core/next-task.ts:479-497 and cross into hench through prd/rex-gateway.ts. Change rex's wording -- \"all children completed, ready to finalize\" to \"all children done, ready to finalize\", say -- and hench's header for a finalize-ready parent silently degrades to \"medium priority\". Nothing throws and no test fails: grepping \"ready to finalize\" across packages/rex/tests, packages/hench/tests and tests/ returns zero assertions on either string, in either package.\n\nREACHABILITY. Every `ndx work` / `hench run` invocation on a parent whose children are all completed, and on any in_progress task. It is the normal display path, not an edge case.\n\nWHY FILED SEPARATELY. Pre-existing; not introduced by the #370-review cleanup (task 79132a2f). That task changed the predicate behind the string (inline `completed || deferred` to the shared allChildrenSuccessful, GH #364) and left the wording alone, so the coupling still works today. It is filed because the cleanup is what made it visible, and because rex's summary text is now the only thing holding hench's display together.\n\nThe deeper problem: a SelectionExplanation carries a rendered sentence but no machine-readable reason code, so the only thing a consumer can do is re-parse prose.\n\nSOLUTION OPTIONS.\n\n1. RECOMMENDED -- add a discriminated reason field to SelectionExplanation in rex (e.g. reason: \"in_progress\" | \"ready_to_finalize\" | \"priority\"), set it where the summary is built, and have hench branch on it. Cost: one additive optional field on a type that already crosses the gateway, plus the hench branch. Risk: low -- summary keeps its current text and every other consumer is unaffected. Removes the class of defect rather than pinning one instance.\n\n2. Pin the strings with a cross-package contract test in tests/integration/ asserting explainSelection still emits the two substrings task-display.ts matches on. Cost: one test file. Risk: makes the coupling permanent and documented rather than removing it; a future reworder now has to update a third place.\n\n3. Export the literals from rex and import them through the gateway. Cost: two constants. Cheapest, but hench still substring-matches a rendered sentence, so a summary that ever embeds one phrase inside another re-opens the same hole.\n\nOption 1 is the right shape: the consumer needs the reason, not the sentence.\n\nACCEPTANCE CRITERIA (could not be written to the acceptanceCriteria field -- see note below; move them there when picking this up):\n1. SelectionExplanation exposes a machine-readable selection reason distinct from the rendered summary string, and explainSelection sets it on all three branches (in_progress, ready-to-finalize, plain priority).\n2. formatSelectionSummary in packages/hench/src/agent/lifecycle/task-display.ts branches on that field and contains no explanation.summary.includes(...) call.\n3. A test asserts that a parent whose children are all completed renders the finalize label in hench, and that rewording rex's summary string does not change what hench renders. This test fails today because hench reads the prose.\n4. The reason field crosses the package boundary through packages/hench/src/prd/rex-gateway.ts rather than by a direct import from rex.\n\nVERDICT: out-of-scope (pre-existing), severity low -- no current failure, but it makes the next wording change riskier.\n\nPROVENANCE: source=ndx-adversarial-review, tags=[ndx-adversarial-review, severity:low]. Found reviewing task 79132a2f-96d9-41f0-8eb9-28d4edf9de59. Created via the rex CLI because MCP writes were unavailable in that non-interactive run, so acceptanceCriteria/tags/source could not be set as structured fields.\n"
lastModified: "2026-09-12T10:39:57.267Z"
lastModifiedBy: "Ryan Keith <ryan.k@endash.us>"
---
