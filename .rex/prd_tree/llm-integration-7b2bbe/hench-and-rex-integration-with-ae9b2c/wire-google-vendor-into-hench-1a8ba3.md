---
id: "1a8ba3b8-82eb-4d90-9079-59810cedae7c"
level: "task"
title: "Wire Google vendor into hench run loop, rex commands, and llm-gateway"
status: "completed"
priority: "critical"
tags:
  - "hench"
  - "rex"
  - "google"
  - "integration"
  - "llm-client"
source: "smart-add"
startedAt: "2026-06-04T19:45:36.240Z"
completedAt: "2026-06-05T18:04:55.512Z"
endedAt: "2026-06-05T18:04:55.512Z"
resolutionType: "code-change"
resolutionDetail: "Wired Google vendor into hench llm-gateway (TIER_MODELS, GOOGLE_MODELS, GoogleConfig exports), fixed resolveAnalyzeTokenEventMetadata in analyze.ts to resolve Gemini model instead of \"unknown\", fixed modelSource determination in prune/reorganize/reshape to include google?.model, added two Google test cases to vendor-model-selection.test.ts."
acceptanceCriteria:
  - "ndx work with llm.vendor=google successfully dispatches agent turns to the Gemini API and produces a completed hench run record"
  - "Vendor header printed at run start shows 'google / <resolved-model-id>'"
  - "rex smart-add and analyze commands use the Google adapter when vendor=google"
  - "Weight-aware model resolution selects the correct Gemini tier model for light, standard, and heavy task weights"
  - "Regression tests for vendor-scoped model selection cover the google vendor branch"
  - "Existing Claude and Codex dispatch paths have no regressions"
  - "Running `rex add`, `rex analyze`, `rex recommend`, `rex reorganize`, `rex reshape`, and `rex prune` with `llm.vendor=google` in .n-dx.json all route LLM calls through the Google adapter without error"
  - "The vendor/model header printed by each command shows the resolved Google model name"
  - "Google quota preflight runs before each command's LLM call and surfaces a clear error on budget exhaustion"
  - "Google-specific LLM errors surface actionable hints via the shared error classifier (rate limit, token exhaustion, bad response)"
  - "Existing vendor=claude and vendor=codex test coverage continues to pass after the change"
  - "`ndx work` completes at least one full tool-use loop with `llm.vendor=google` set without falling back to another vendor"
  - "Token usage from a Google-vendor hench run is recorded in `.hench/runs/` and attributed to the active PRD task"
  - "The hench run output prints the resolved Gemini model name in the vendor/model header"
  - "Failover from Google to the next chain member triggers correctly on a quota or auth error"
  - "llm-gateway re-exports the Google model constants and resolver functions needed by the agent loop"
description: "All rex commands that invoke an LLM — smart-add, analyze, recommend, reorganize, reshape, and prune — must respect `llm.vendor=google` and route their LLM calls through the Google adapter via the centralized resolveVendorModel resolver. Currently these commands skip Google even when it is set as the active vendor, falling back to Claude or Codex. Update each command's LLM invocation site in rex to pass the resolved Google model, include the vendor header in console output, and surface Google-specific error hints through the shared error classifier."
---
