---
id: "25a61f86-7ee3-42bf-9752-e3c89a8c5433"
level: "task"
title: "Address suggestion issues (13 findings)"
status: "completed"
priority: "critical"
source: "sourcevision"
startedAt: "2026-03-08T04:33:13.504Z"
completedAt: "2026-03-08T04:45:17.760Z"
acceptanceCriteria: []
description: "- Add a gateway-contract.test.ts per consumer package (hench, web) that statically asserts the exported symbol set of each gateway file matches the expected interface. This fills the gap between 'gateway file exists' (scripts) and 'CLI behavior is correct' (e2e) with a fast, precise contract layer that catches upstream renames before any process is spawned.\n- Six web server route and index files directly read or write .rex/prd.json via readFileSync/writeFileSync, bypassing rex-gateway.ts entirely. This is the most critical gateway-pattern violation in the codebase: it creates six undocumented write channels to shared PRD state, each skipping rex's store-level locking and schema validation. Audit and route all web prd.json access through rex-gateway.ts.\n- The packages/web/src/server/ directory contains 30 files from three zones with no subdirectory grouping. Apply a directory-per-zone convention by moving task-usage analytics files into server/usage/, mcp route files into server/mcp/, and infrastructure utilities (aggregation-cache, concurrent-execution-metrics, pr-markdown-refresh-diagnostics, process-memory-tracker) into server/infra/. This would make zone membership discoverable without running sourcevision analysis.\n- The two enforcement zones (monorepo-maintenance-scripts, cli-e2e-tests) have no shared documentation of which architectural rules each owns. Add a single ENFORCEMENT.md at the monorepo root that maps each architectural constraint to its enforcement mechanism (tsc / maintenance script / architecture-policy test / e2e test), preventing future contributors from adding duplicate or conflicting enforcement for the same rule.\n- packages/claude-client/ contains no source files — only a stale /dist/ and /node_modules/ from a completed migration to @n-dx/llm-client, with zero consumers in the codebase. Remove the directory to eliminate false package-count inflation and contributor confusion about which foundation package is active.\n- 2 zones exceed architectural risk thresholds (cohesion < 0.4, coupling > 0.6): web-build-infrastructure, packages-rex:prd-validation — mandatory refactoring recommended before further development\n- Rename the 'web-dashboard' zone to 'web-composition-root' or 'web-server-core' to accurately reflect its role as the application composition root rather than implying it only contains dashboard UI code. The current name actively misleads contributors about where server infrastructure, CLI entrypoint, and route dispatch logic live.\n- The 'ndx usage' and 'ndx sync' commands have no obviously dedicated e2e test file. Confirm coverage exists in cli-delegation.test.js or add cli-usage.test.js and cli-sync.test.js to make coverage intent explicit and discoverable by file name.\n- The zone has coupling 0.6 with only 3 files, meaning over half of its inter-file edges cross the zone boundary into web-dashboard. At this coupling density, any refactor of the two files it imports from web-dashboard (start.ts or rex-gateway.ts) has a direct probability of breaking this zone — add a coupling threshold alert or zone-level import test to make this fragility visible before refactors.\n- config.js implements config-merging logic (deepMerge, loadJSON, saveJSON, validators) and directly reads/writes package config files rather than delegating to a spawned CLI, violating the orchestration tier's documented spawn-only rule. This is the only root orchestration script that breaks this invariant. Either extract a dedicated config subcommand in a domain package, or explicitly document config.js as a spawn-exempt exception in CLAUDE.md.\n- Six web server files (routes-rex.ts, routes-validation.ts, routes-hench.ts, routes-status.ts, routes-data.ts, search-index.ts) bypass rex-gateway.ts and directly read/write .rex/prd.json via readFileSync/writeFileSync. This creates six undocumented mutation channels that skip any locking, schema validation, or migration logic in rex's store module. All prd.json access in the web package must be routed through rex-gateway.ts to close this gap.\n- Zone \"Web Build Infrastructure\" (web-build-infrastructure) has critical risk (score: 0.67, cohesion: 0.33, coupling: 0.67) — requires refactoring before new feature development\n- Zone \"PRD Validation\" (packages-rex:prd-validation) has critical risk (score: 0.66, cohesion: 0.34, coupling: 0.66) — requires refactoring before new feature development\n\n---\n\n- A vitest config-level include pattern that restricts tests/integration/ to **/*.test.js (rejecting .ts files) would enforce the dist/-only import guarantee automatically and catch extension mistakes in CI rather than silently degrading the zone's fidelity contract.\n- Intra-package gateway modules (web external.ts and api.ts) are not validated for the re-export only no-logic invariant that domain-isolation.test.js enforces for cross-package gateways. Extending the gateway logic check to cover intra-package gateways would close a systematic enforcement gap and make the policy symmetric across all gateway types.\n- No CI gate enforces a minimum cohesion threshold for source zones. Zones below 0.5 cohesion (fix: 0.25, web-shared: 0.46, crash: 0.5) are flagged only in analysis reports. Adding a zone health gate to ci.js (minimum cohesion 0.5 for source zones with 5 or more files) would prevent silent accumulation of incoherent utility zones over time.\n- The server and viewer independently define the FeatureToggle API contract shape (routes-features.ts and views/feature-toggles.ts). Audit all other API response types defined in server route files to identify additional silent duplications in viewer code — the FeatureToggle case is likely not unique. A one-time grep for interface/type definitions in routes-*.ts whose fields also appear in viewer/views/*.ts would surface the full duplication set.\n- The web package has a shared-types.ts file in the server zone whose viewer-consumed types are the probable source of the 32 web-viewer→web-server import edges. A systematic audit of shared-types.ts exports — classifying each as server-only, viewer-only, or framework-agnostic — would produce a concrete relocation plan: server-only types stay, viewer-only types move to viewer/types.ts, and framework-agnostic types move to web-shared. This single audit would likely resolve the 32-edge anomaly that has been flagged across multiple prior enrichment passes.\n- web-shared satisfies both fragility criteria (cohesion: 0.46, coupling: 0.54) while serving as the three-consumer foundation of the web package. Establish a formal addition policy for web-shared: a new module should have at least two distinct consumer zones before being added to web-shared; single-consumer utilities belong closer to their dominant use site. Document this in CLAUDE.md under the web-shared zone description.\n- Zone-confirm that packages/web/src/schema/v1.ts is in web-viewer (not web-server). If confirmed, the 32 web-viewer→web-server edges are genuine server-zone type imports by viewer components, not schema imports. This makes relocating the source of those 32 imports (likely types.ts and shared-types.ts) to web-shared the highest-leverage single action to resolve the 32-edge coupling anomaly.\n- Add a top-of-directory README.md or a comment block in each test file explicitly documenting the .js-extension convention and the reason (prevent TypeScript alias resolution, preserve dist/-only boundary). Without this, the next contributor adding an integration test will default to .ts and silently disable the zone's core guarantee.\n- The web peripheral zone combines cohesion 0.55 with bidirectional imports from both web-viewer (15 edges) and web-server (2 edges) — high coupling AND low cohesion simultaneously. Audit whether the 15 web-viewer imports can be reduced by extracting shared types to web-shared; this would raise cohesion and lower coupling in a single pass.\n- FeatureToggle and FeaturesResponse are duplicated between routes-features.ts (server) and views/feature-toggles.ts (viewer). The viewer definition was written from scratch rather than sharing the server's type — a concrete manifestation of the missing server/viewer contract layer. Extract both interfaces to src/schema/v1.ts or a new src/schema/features.ts and import from there in both files. This resolves a silent drift risk: a server field change currently requires a manual viewer update with no compile-time failure.\n- packages/web/src/server/shared-types.ts is a probable root cause of the 32 web-viewer→web-server edges: if viewer components import server-side shared types from this file, those imports register as viewer→server boundary crossings. Moving framework-agnostic types from shared-types.ts into web-shared would allow viewer to import them without crossing the server zone boundary and would reduce the 32-edge count to zero for those specific imports.\n- external.ts is missing a re-export for SUPPLEMENTARY_FILES from shared/data-files.ts. shared/index.ts exports all three constants (DATA_FILES, ALL_DATA_FILES, SUPPLEMENTARY_FILES) but external.ts only surfaces the first two. Add SUPPLEMENTARY_FILES to external.ts to close the gap and prevent future viewer code from bypassing the gateway with a direct shared/ import to access supplementary file names.\n- web-shared has cohesion 0.46 AND coupling 0.54 — the only production zone meeting both the low-cohesion and moderate-coupling thresholds simultaneously. This combination makes it fragile: changes have wide blast radius while the zone lacks the internal coherence to absorb or predict those changes. Treat web-shared as a risk zone requiring active governance on additions.\n\n---\n\n- Add a subsection to CLAUDE.md's Architecture section documenting the web package's internal zone layering: web-shared (foundation) → viewer-messaging-stack (messaging middleware) → web-viewer (consumer), with web-server as a parallel composition-root tier. The four-tier monorepo hierarchy is documented but the web package's own internal stack is not, leaving developers without a documented model for intended import direction within the package.\n- Three web-package zones (web-server: 4 files, web-shared: 3 files, logo: 2 files) are below a meaningful minimum zone size for health metric reliability. Cohesion and coupling calculations on fewer than 5 files produce high-variance readings that change dramatically with single-file additions. Define a minimum zone size threshold of 5 files below which health metrics are suppressed or annotated as unreliable in zone health reports.\n- 3 zones exceed architectural risk thresholds (cohesion < 0.4, coupling > 0.6): request-deduplication, viewer-message-pipeline, packages-rex:validate — mandatory refactoring recommended before further development\n- hench-agent spans 31 directories with no sub-zones, while the web package applies sub-zone decomposition at roughly 60 files per sub-zone. At 155 files, hench-agent qualifies for at least 2-3 sub-zones (e.g. agent-core, store, prd-integration, guard-policy). Without sub-zones, any zone-level coupling finding has a 155-file blast radius and directory-level architectural drift is invisible to sourcevision health metrics.\n- The deleted .sourcevision/zones/mcp-route-layer/ directory in the git status (shown as D in the diff) confirms that 'mcp-route-layer' was previously an explicit zone ID for domain-gateway.ts and routes-mcp.ts. The 4-file sub-zone should be re-pinned under 'mcp-route-layer' to restore the semantically correct zone name that was lost.\n- All existing zone insights for request-deduplication that describe 'two implementations', 'split consumer bases', and a 'causally linked atomic change' are based on the false premise that shared/request-dedup.ts contains an independent implementation. Update these insights to reflect the actual state: one implementation (viewer/messaging/), one dead shim (shared/), one correct test. The remediation complexity has been significantly overstated.\n- packages/web/src/shared/request-dedup.ts is dead code: it re-exports from viewer/messaging/request-dedup.ts, and shared/index.ts already re-exports the same symbols directly from the same canonical source. No source file imports from shared/request-dedup directly. Delete the shim file in a single commit — no consumer updates, no test changes, no import path redirects are needed.\n- Zone \"Request Deduplication\" (request-deduplication) has critical risk (score: 0.67, cohesion: 0.33, coupling: 0.67) — requires refactoring before new feature development\n- Add mcp-transport.test.js and integration-coverage-policy.test.js to CLAUDE.md's Key Files table alongside architecture-policy.test.js and domain-isolation.test.js. All four files form the project's architectural enforcement corpus — omitting two of them from the Key Files table creates an incomplete picture of enforcement coverage for developers consulting documentation.\n- Zone 'viewer-message-pipeline' is the sole zone with both high coupling (0.65) AND low cohesion (0.35) simultaneously — a fragile combination where changes have wide blast radius and internal structure provides no stabilizing guidance. This risk profile warrants priority resolution before adding any new files that import from this zone, as the coupling score will increase further. Resolve by zone-pinning the misclassified build scripts to web-dashboard and viewer utilities to web-viewer, then verify coupling drops below 0.3.\n- Zone \"Viewer Message Pipeline\" (viewer-message-pipeline) has critical risk (score: 0.65, cohesion: 0.35, coupling: 0.65) — requires refactoring before new feature development\n- Zone \"Analyzers 3\" (packages-sourcevision:analyzers-3) has catastrophic risk (score: 0.73, cohesion: 0.27, coupling: 0.73) — requires immediate architectural intervention\n- Zone \"Validate\" (packages-rex:validate) has critical risk (score: 0.64, cohesion: 0.36, coupling: 0.64) — requires refactoring before new feature development"
recommendationMeta: "[object Object]"
---
