---
id: "c7d5bc62-c804-4be5-bdf9-b366da18c576"
level: "task"
title: "Allowlist workspace.ts in the child_process architecture policy (PR #354 CI fix)"
status: "pending"
priority: "critical"
acceptanceCriteria: []
description: "CI FIX for PR #354 (hotfix/sv-analyze-skip-worktree-scans). The branch is red on one test.\n\nFailure: `tests/e2e/architecture-policy.test.js > architecture policy: process execution > no direct child_process imports outside allowed files`\n\n  Direct child_process imports found outside allowed files.\n  Violations:\n    - packages/sourcevision/src/analyzers/workspace.ts\n\nCause: the worktree-skipping fix on this branch added `import { execFileSync } from \"node:child_process\"` to `packages/sourcevision/src/analyzers/workspace.ts` so `listNestedWorktrees()` can call `git worktree list --porcelain`. The policy test forbids direct `child_process` imports outside an explicit ALLOWED set.\n\nRequired change: add `\"packages/sourcevision/src/analyzers/workspace.ts\"` to the `ALLOWED` set in `tests/e2e/architecture-policy.test.js`, inside the existing \"Git operations — need execFileSync/execFile for git CLI calls\" group (currently around lines 63-69, alongside `branch-work-collector.ts`, `branch-work-filter.ts`, `git-credential-helper.ts`, `prd-epic-resolver.ts`). Add a brief comment on the entry explaining why: `detectSubAnalyses()` is synchronous and must stay synchronous, so it needs `execFileSync` for its one best-effort `git worktree list --porcelain` call; the llm-client `exec()` / `execStdout()` helpers are Promise-returning and converting the analyzer to async would ripple through every caller.\n\nRationale for this route over the alternatives (do not implement these, they are recorded so the choice is auditable):\n- `@n-dx/llm-client`'s general-purpose exec helpers (`exec`, `execStdout`, `execShellCmd`) all return Promises. Only purpose-specific sync helpers exist there (`getCurrentHead`, `getCurrentBranch`, `isExecutableOnPath`) and none of them lists worktrees.\n- Making `detectSubAnalyses()` async would change a public analyzer signature and cascade to its callers — unacceptable churn for a hotfix aimed at a fast merge.\n- The ALLOWED list already exempts two sibling analyzers for exactly this reason, so this is the established precedent in this repo, and the policy test's own failure message names adding to ALLOWED as the supported escape hatch.\n\nConstraints:\n- Do NOT modify `packages/sourcevision/src/analyzers/workspace.ts`. The analyzer logic is correct and already verified; only the policy allowlist needs updating.\n- Do NOT add any other file to ALLOWED, and do not reorder or reformat unrelated entries.\n- Keep the diff to the single ALLOWED insertion plus its comment.\n\nAcceptance criteria:\n- `tests/e2e/architecture-policy.test.js` passes in full, including the sibling \"ALLOWED list contains no stale entries (all files exist on disk)\" test.\n- The full root test suite passes: run `pnpm test` from the repo root (it routes through `scripts/run-all-tests.mjs`) and confirm zero failures. This is the check that was skipped last time and let the violation reach CI, so it must actually be run, not assumed.\n- No changeset is needed: this touches only a test policy list, not shipped package code."
lastModified: "2026-09-07T22:33:04.111Z"
lastModifiedBy: "Ryan Keith <ryan.k@endash.us>"
---
