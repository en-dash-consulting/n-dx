---
id: "cbb8a5de-d037-4353-acd9-a0f4b4d41168"
level: "task"
title: "ArchitectureView calls useMemo after its enrichment-gate early return"
status: "completed"
priority: "medium"
tags:
  - "ndx-adversarial-review"
  - "severity:medium"
  - "web"
  - "viewer"
  - "correctness"
source: "ndx-adversarial-review"
startedAt: "2026-09-10T17:24:13.335Z"
completedAt: "2026-09-10T17:30:21.397Z"
endedAt: "2026-09-10T17:30:21.397Z"
resolutionType: "code-change"
resolutionDetail: "Hoisted the zoneHealthData useMemo above the enrichment gate in architecture.ts (solution option 1). Went further than option 1 on the guard: instead of only appending architecture.ts to the hardcoded GATED_VIEWS list, added a completeness assertion that reads the views directory for `h(EnrichmentGate` and asserts the list equals it — closing the class that let this instance escape, which was option 2's goal at a fraction of its cost (no generalisation of the crude componentBody parser needed, since the list is still explicit; only its completeness is derived). All four acceptance criteria met: structural guard red before the fix with exactly one failure and green after, transition tests across the threshold in both directions and repeatedly, and a dedicated assertion that the zone-health chart renders above the threshold and not below. Build, typecheck, pnpm validate and the full root suite green; web 3549 -> 3555 tests."
acceptanceCriteria:
  - "ArchitectureView calls the same hooks in the same order on every render, gated and ungated — no hook call appears after the EnrichmentGate early return"
  - "architecture.ts is listed in GATED_VIEWS in packages/web/tests/unit/viewer/enrichment-gate-hook-order.test.ts, and that assertion fails against the current code and passes after the fix"
  - "A test mounts ArchitectureView below the architecture threshold (enrichmentPass < 2) and re-renders it above the threshold without hook-order corruption"
  - "The enrichment gate still renders when enrichmentPass is below 2, and the zone-health chart still renders unchanged when it is at or above 2"
description: "Severity: medium. Verdict: should-fix (real, reachable, cheap fix, same defect class the branch declares fixed).\n\n## The defect\n\n`ArchitectureView` is the third pass-gated SourceVision view and has the exact shape that cbcc2814 (\"Fix conditional hook order in the Problems and Suggestions views\") fixed in the other two — but it was left unfixed, and the structural guard added alongside that fix does not cover it.\n\n- `packages/web/src/viewer/views/architecture.ts:20` — `return h(EnrichmentGate, ...)` when `enrichmentPass < ENRICHMENT_THRESHOLDS.architecture` (2)\n- `packages/web/src/viewer/views/architecture.ts:42` — `const zoneHealthData = useMemo(...)`, below that return\n- `packages/web/tests/unit/viewer/enrichment-gate-hook-order.test.ts:43` — `GATED_VIEWS` is hardcoded to `problems.ts` and `suggestions.ts`\n\n`grep -l EnrichmentGate packages/web/src/viewer/views/` returns three files; `ENRICHMENT_THRESHOLDS` has exactly three keys. Architecture is the omitted one of three.\n\nThe guard's own `describe` reads \"gated views call every hook before they can return\" — a claim a two-entry hardcoded list cannot support. The suite is green with the defect present.\n\n## Failure scenario\n\n`sidebar.ts:376` keeps locked views navigable. Open Architecture while `zones.enrichmentPass` is 0: the gate renders and `useMemo` is never called, so the component's hook list is empty. An analysis finishes with the dashboard still open, `enrichmentPass` becomes 2, and the same mounted component re-renders down the non-gated path — a `useMemo` slot appears at position 0 where there was none.\n\nToday nothing is misread, because the hook list grew from zero — this is latent, exactly as the changeset for the sibling fix describes (\"the exposure is to the next edit\"). That was equally true of ProblemsView and SuggestionsView before this branch, and the guard's docblock correctly argues it is not a reason to leave it. The exposure is the next edit that adds any hook ABOVE line 20 — a `useFeatureToggle`, `useToast`, `useRouteState`. Slot 0 then means the newly added hook on a gated render and `zoneHealthData` on an ungated one. Preact matches hooks positionally and, unlike React, emits no warning: the slots silently shift under whatever state is already there.\n\nNote this is precisely how the sibling defect was introduced — cbcc2814's own description records that adding `useFeatureToggle` for the Explain action is what surfaced it.\n\n## Reachability (Pass 2)\n\nReachable through a real entry point: the Architecture tab in the dashboard sidebar, on any project whose analysis has not yet reached enrichment pass 2. Rendered as a true component (`view-registry.ts:118` uses `h(ArchitectureView, ...)`), so the hooks are its own rather than the parent's — which is what keeps this latent rather than an immediate live bug.\n\nNot covered by anything: no test renders `ArchitectureView` across its threshold, and the structural guard's list omits the file. Full build, typecheck, and all package + root test suites are green with the defect present.\n\n## Solution options\n\n1. **Move the memo above the gate and extend the guard list (recommended, do this first).** Hoist the `zoneHealthData` useMemo above the early return, mirroring the two fixed views, and add `{ name: \"architecture.ts\", component: \"ArchitectureView\" }` to `GATED_VIEWS`. ~4 lines. Cost: near zero. Risk: none — `zoneHealthData` already guards `if (!zones) return []`, so computing it on a gated render is safe and cheap. Add the guard entry FIRST so the test fails before the fix and passes after, proving it has teeth.\n\n2. **Derive `GATED_VIEWS` by scanning `views/` for `EnrichmentGate` rather than listing them.** This closes the class rather than the instance, so a fourth gated view is covered the moment it appears and cannot be added unguarded.\n\n   There is a precedent for exactly this, added by this same branch: `tests/helpers/all-skills.js` `getLocalSkillNames()` derives the repo-local skill list by subtracting the manifest from `.claude/skills/`, with the docblock \"Derived by subtraction rather than hardcoded, so a new repo-local skill is covered the moment its directory appears.\" Its own rationale is the same failure this item describes — three skills sat outside every content guard, invisibly, because the list was derived from the wrong source.\n\n   Cost: moderate. The guard's `componentBody` helper is deliberately crude and assumes one exported component per file; that holds for the three views today but needs care when generalised. Risk: a scanner that silently matches nothing is worse than a list, so it needs a floor assertion (at least three views found). Recommended as a follow-up on top of option 1, not instead of it — option 1 fixes the live gap, option 2 stops it recurring.\n"
lastModified: "2026-09-10T17:30:21.403Z"
lastModifiedBy: "sterling.h@endash.us <sterling.h@endash.us>"
---
