---
id: "50f24186-8c11-4eb2-bbca-9d83a731ab47"
level: "task"
title: "Confirm AC 7 of the freeze-verify-kill task: a POSIX CI run must actually execute the freeze policy green"
status: "completed"
priority: "medium"
tags:
  - "cross-os"
  - "process-lifecycle"
  - "llm-client"
  - "ci"
  - "verification"
source: "margin-audit-2026-08-19"
startedAt: "2026-08-19T16:32:56.191Z"
completedAt: "2026-08-19T17:27:48.172Z"
endedAt: "2026-08-19T17:27:48.172Z"
resolutionType: "code-change"
resolutionDetail: "AC 7 of 71e44890 satisfied on POSIX via ubuntu: PR en-dash-consulting/n-dx#329, sha bc90582c, run 32279143889, job \"Build & Validate\" step \"Run unit tests\" success, which runs llm-client's integration suite including both freeze policies. Confirmed by step conclusion plus structure, not by reading the log line (log download is 403 without admin); the silent-skip risk is ruled out separately. AC 2's macOS clause corrected as unsatisfiable — the macos-latest job runs only the root suite and never executes this test. Also reconciled TESTING.md, which did not merely lack guidance on complexity gates but banned them: Family 2 is now a graded hierarchy (count work, then assert growth, then scaled absolutes as hang guardrails only), plus a new rule against scaling a bound whose job is to sit below another number, and a flush-before-retry rule."
acceptanceCriteria:
  - "DONE. The branch carrying 14322618 is pushed and a full CI run completed. Required rebasing onto origin/main (2 new commits, 266 files) and force-pushing; head bc90582c, backup at backup/pre-main-rebase-windows-startup. The run is on PR en-dash-consulting/n-dx#329 — note the PR lives in the n-dx repo, NOT n-dx-internal, whose ci.yml only triggers on push-to-main and pull_request and therefore shows zero runs for this branch"
  - "DONE, with the macOS half CORRECTED as unsatisfiable-as-written. Run 32279143889, all 4 checks success. The freeze test runs wherever `run-all-tests.mjs packages` runs, which is \"Build & Validate\" (ubuntu-latest, step 12 \"Run unit tests\") and \"CLI Smoke (Windows)\" (step 10 \"Run per-package tests\") — both success. It does NOT run on macOS: the macos-latest job has no per-package step at all, only the root suite. So ubuntu is the POSIX evidence and macOS never could have been. Confirmed by step conclusion plus structure rather than by the log line: job-log download returns 403 without admin rights"
  - "DONE. Two candidate skip causes were ruled out before the run (run-all-tests.mjs discovers every package with a test script; run-vitest-bind-aware.mjs excludes only ROOT-profile suites), and two more after (the describe.each over both policies is unconditional and not env-gated, and all 6 cases were observed executing locally). No filtering is in play"
  - "DONE. Windows passed the per-package step too, and is explicitly NOT counted — the freeze parameter is inert there, so its green proves only that requesting freeze does not break the Windows path"
  - "DONE. AC 7 on 71e44890 now records the satisfying run (PR #329, sha bc90582c, run 32279143889, ubuntu \"Build & Validate\"), the admin-rights caveat on log confirmation, and the fact that macOS never executes this test"
description: "Closes the last open acceptance criterion of 71e44890-a3b3-4a24-aa15-4aefb94c8735 (Freeze-verify-kill: make the POSIX tree kill definitive for timeouts), which is marked completed but whose AC 7 reads:\n\n  \"Both branches are exercised on any host via injected seams, AND the POSIX path is\n   proven against real processes in CI — injected coverage alone is what let the\n   previous defect ship\"\n\nSTATUS 2026-08-19: AC 1 done. ACs 2-5 blocked on tooling, see BLOCKER below.\n\nTHE GAP IS NARROWER THAN IT LOOKS — checked, so nobody re-derives this:\n\nThe test already exists and is correctly wired. packages/llm-client/tests/integration/exec-timeout-tree-kill.test.ts (added by 14322618) runs describe.each over two policies, `{ freeze: undefined }` and `{ freeze: true }`, and passes `freeze` PER CALL rather than through the environment. So it does not depend on NDX_POSIX_FREEZE_KILL being set, and an ordinary CI run does enter the BETA freeze path against a real process tree. No CI configuration change is needed.\n\nCI does cover it: the ubuntu-latest and macos-latest jobs in .github/workflows/ci.yml both run `node scripts/run-all-tests.mjs packages`, llm-client's test script is plain `vitest run`, and its vitest config includes `tests/**/*.test.ts` — so tests/integration is in scope.\n\nVERIFIED LOCALLY: all 6 cases execute and pass on Windows, so the case is not being filtered out. That retires the risk behind AC 3 but proves nothing about freeze semantics, which are inert on Windows.\n\nBLOCKER — WHY THIS IS STILL OPEN: reading the CI run needs access this machine does not have. `gh` is not installed, no GH_TOKEN/GITHUB_TOKEN is set, and the repo is private so the Actions API cannot be queried unauthenticated. Local POSIX substitutes are also unavailable: WSL has only the docker-desktop utility distro and Docker's Linux daemon is not running. Unblock by any one of: (a) someone with CI access reads the run and reports it, (b) install `gh` or provide a token, (c) start Docker's Linux engine so the suite can run in a node:22 container — that is real POSIX evidence, though not literally \"in CI\".\n\nDO NOT ACCEPT A GREEN TOTAL AS PROOF. A passing overall run does not show that the freeze case executed rather than being skipped — a describe.each case that never ran leaves no trace in a summary line. Confirm it by name.\n\nCORRECTION to the original filing: it warned that run-vitest-bind-aware.mjs might exclude this suite. It cannot — that runner only takes exclusions for the ROOT profile, and this is a package test. Ruled out, not a live risk."
---
