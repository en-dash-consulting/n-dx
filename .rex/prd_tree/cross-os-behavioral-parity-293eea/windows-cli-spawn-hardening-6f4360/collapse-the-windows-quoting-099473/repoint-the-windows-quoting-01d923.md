---
id: "01d923cb-e8b0-49d1-bd33-9a57b1d4ec9e"
level: "subtask"
title: "Repoint the Windows quoting parity test at src/exec.ts instead of dist/public.js"
status: "completed"
priority: "high"
tags:
  - "windows"
  - "quoting"
  - "testing"
  - "llm-client"
  - "quick-win"
source: "exploration-2026-08-17"
startedAt: "2026-08-17T18:01:55.779Z"
completedAt: "2026-08-17T18:18:21.505Z"
endedAt: "2026-08-17T18:18:21.505Z"
resolutionType: "code-change"
resolutionDetail: "Repointed the parity test at packages/llm-client/src/exec.ts (bc7d413a). Both negative checks verified: green with dist reverted to the old divergent builder, red when the source twins genuinely diverge. Also fixed two pre-existing failures per workflow step 5 — GNU tar drive-letter parsing in published-package-loadability (ba2d106c) and a CRLF-shebang collection failure in obfuscated-code-policy (worktree re-checkout, no commit) — plus an incremental-build false positive in my earlier dist-staleness check (3579538b)."
acceptanceCriteria:
  - "tests/unit/windows-quoting-parity.test.js imports the llm-client twin from packages/llm-client/src/exec.ts, not dist/"
  - "The test passes with a deliberately stale dist/ present (delete or revert packages/llm-client/dist/exec.js and confirm it still passes) — proving build-independence"
  - "The test still FAILS when the two source implementations are made to genuinely diverge (temporarily edit one twin to confirm red)"
  - "The file header comment explains why this guard reads source rather than compiled output"
  - "The config.js backward-compat parity assertion is unchanged and still green"
description: "tests/unit/windows-quoting-parity.test.js:24 imports `buildWindowsCliCommandLine` from `../../packages/llm-client/dist/public.js` while line 25 imports the core twin from source (`packages/core/win-spawn.js`). Comparing a source-side twin against a compiled-artifact twin means an unbuilt change to exec.ts manufactures a false divergence — exactly what happened: dist still carried the pre-`WINDOWS_BARE_BINARY_RE` builder, producing `\"claude\" \"--print\" \"hi\"` against source's `claude \"--print\" \"hi\"`, and the failure read as a package-vs-package quoting bug rather than \"your build is stale\".\n\nFix: import from `packages/llm-client/src/exec.ts`. Vitest transforms TS directly, so no build step is involved and the guard compares source to source. `quoteWindowsToken` and `buildWindowsCliCommandLine` are already exported from exec.ts (not just re-exported via public.ts), so the import needs no new export.\n\nThe file header comment currently justifies the dist import with \"root tests run against compiled artifacts; the vitest globalSetup verifies the build exists\" — reasonable for integration tests that spawn real CLIs, but a PURE-FUNCTION parity guard has no reason to care about the build. Update that comment to state the new rationale so nobody repoints it back at dist.\n\nKeep the third import (`packages/core/config.js`, the backward-compat re-export check) as-is — it is already source-side."
---
